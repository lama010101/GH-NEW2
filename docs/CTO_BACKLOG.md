# CTO BACKLOG & BUG TRACKER
**Project:** Guess-History (GH-NEW)
**Maintained by:** Claude (CTO) — updated via coder prompts, never edited directly by coders without instruction
**Status:** ACTIVE — single source of truth for bug/task status, supersedes long-form status text in Claude's memory

---

## HOW THIS FILE WORKS

- Every coder prompt that resolves, advances, or discovers a bug/task MUST include a validation step instructing the coder to update the relevant entry below (status + one-line evidence + commit hash).
- Status values: `OPEN` (not investigated) · `INVESTIGATING` (A0/B in progress) · `APPROVED-FOR-FIX` (Phase C passed) · `FIX-APPLIED` (committed, not re-verified) · `CLOSED` (Phase E re-verified) · `PARKED` (deprioritized, not abandoned).
- Do not delete closed items — move them to the "CLOSED LOG" section at the bottom. History matters for this project (see process gap MP-INV-HARNESS-BLOCKER-STATUS-001).
- Claude (CTO) is the only one who changes an item's status in conversation; the coder only proposes evidence in the file via prompted instruction.

---

## SECTION A — MULTIPLAYER TEST HARNESS (MP-AUTO-TEST-001)

### Harness Bug Tracker

| # | Bug | Status | Commit | Notes |
|---|-----|--------|--------|-------|
| 1 | host()-call bug | CLOSED | 77efc97 | Confirmed fixed |
| 2 | gameId-param bug | CLOSED | 60f7119 | Confirmed fixed |
| 3 | kick-rejoin bug | CLOSED | 5433acd | Confirmed fixed |
| 4 | sleep-prevention bug | CLOSED | 5433acd | Confirmed fixed |
| 5 | playerId-empty (globalSetup/test-process split) | CLOSED | a71b9c2 | File-based handoff via `.test-user-ids.json` |
| 6 | hostClient early-capture in `runGame()` | CLOSED | (same as #5) | Fixed by playerId-empty PR |
| 7 | hostClient early-capture in `playAgain()` | **INVESTIGATING (A0 in progress)** | 97d2837 (fix applied, NOT verified) | CTO REJECTED coder's self-contradictory "1 passed" / "Overall FAILED" report. Phase A0 required: investigate browser-closure errors (`Target page, context or browser has been closed`) during playAgain()'s game transition (2nd/3rd game in suite) — specifically shared `chromiumBrowser`/`webkitBrowser` in `beforeAll`/`afterAll` across sequential tests in `multiplayer-simulation.spec.ts`. Root cause HYPOTHESIS (unconfirmed): Test 1's `afterAll` force-closes browsers Test 2 then inherits. Proposed fix: move browser declarations to local per-test scope w/ try/finally. Phase B execution attempt on commit `216c916` REJECTED — structurally correct but zero validation ran (node/npm/npx not on PATH; nvm-prefixed follow-up issued). |
| 8 | `resume-after-refresh` test — `wsClients[0]` undefined at `gameOrchestrator.ts:153` | **OPEN — needs full log + code read** | none | Attempt 1 hangs silently after lobby state; retry throws `TypeError: Cannot read properties of undefined (reading 'waitForState')`. Suggests `initClients()` didn't populate `wsClients`. Log was truncated — need full untruncated log + read of `initClients()`/`runGame()` lobby-wait sequence before any fix attempt. |
| 9 | local dev session join hang against production PartyKit | **OPEN — investigation complete** | none | Local dev (npm run dev) connects to production PartyKit (guess-history-multiplayer.lama010101.partykit.dev). JOIN_ROOM handler calls back to https://guess-history.com/api/compete/{gameId}/join (NEXTJS_BASE_URL). Production Next.js queries production Supabase (gzvixlvkwjsrtmtybtkf) for gameId created in local dev — gameId not found, join fails silently. |
| 10 | capture actual JOIN_ROOM API response when local session hangs | **OPEN — investigation complete** | none | UNDETERMINED: PartyKit runs on Cloudflare, console output not accessible from local machine. npx partykit tail connected but showed no logs (hang not reproduced during investigation window). PARTYKIT_SECRET matches between .env.local and production. /join route returns 401 on secret mismatch, 404 on "Session not found", 400 on other errors. Without reproducing the actual hang, real response cannot be determined. |
| 11 | capture real Vercel production logs for JOIN_ROOM /join call during live repro | **OPEN — investigation complete** | none | NO LOGS CAPTURED: Vercel CLI authenticated (lama010101). npx vercel logs guess-history.com --follow streamed for ~90 seconds while local dev server ran on port 3000. No log lines containing [JOIN_NOTIFY_TARGET], [JOIN_ROUTE_START], [JOIN_DB_INSERT_OK], or [JOIN_ROUTE_ERROR] appeared. The hang was not reproduced during the investigation window, so the actual /join response status code remains undetermined. |
| 12 | production deployment health check (all recent deployments show Error) | CLOSED (MP-INV-PROD-DEPLOY-HEALTH-001) | none | (a) Production NOT broken: guess-history.com points to deployment dpl_27RqCkp4NzPGo1DKRdaihXUxf3VE with status "● Ready" (created 24h ago). Recent deployments fail due to ESLint errors in src/core/rules.correctness.test.ts (unused functions calculateLocationAccuracy, calculateYearAccuracy). (b) /join endpoint works: curl to https://guess-history.com/api/compete/{gameId}/join with valid gameId returned 200 with full session data (no hang). Live logs show [JOIN_ROUTE_START] and [SNAPSHOT_PLAYERS] entries for active games. |
| 13 | correctly verify JOIN_ROOM /join response for a single traceable gameId | CLOSED (MP-INV-LOCALDEV-PARTYKIT-MISMATCH-004) | none | 2026-06-26: Prior MP-INV-PROD-DEPLOY-HEALTH-001 Step 4 conclusion REJECTED as INVALID — it used wrong gameId in logs (b6b54a79 vs f64305fb) and byte-identical /create vs /join response suggested stale/cached data. CORRECTED VERIFICATION: Created fresh session gameId 912deb1e-797e-40b2-87c3-6dfd8f578593 via /create. /join WITHOUT -L showed 308 redirect to www.guess-history.com. Following redirect manually returned HTTP 200. Response body showed 2 players (including new playerId 00000000-0000-0000-0000-000000000002). Vercel logs confirmed [JOIN_ROUTE_START] and [SNAPSHOT_PLAYERS] entries for this exact gameId. CONCLUSION: /join DOES successfully add second players; prior "failure" was due to gameId mismatch in log analysis, not an actual endpoint bug. |

### Harness Infrastructure (CLOSED/verified facts — do not re-investigate)
- Harness file committed on `main`: `scripts/test/playwright/specs/multiplayer-simulation.spec.ts` 
- `PLAYWRIGHT_BASE_URL` env var (not .env.local's app config) controls target — was never set, defaulted to `localhost:3000`. Correct invocation:
  - Prod: `PLAYWRIGHT_BASE_URL=https://www.guess-history.com npm run test:simulation` 
  - Local: `npm run dev` in one shell + `npm run test:simulation` in another
- WS transport fix confirmed end-to-end: commit `aab6e49` 
- Production PartyKit host confirmed healthy: `guess-history-multiplayer.lama010101.partykit.dev`, party `"lobby"` 
- Execution environment: Devin Local (MacBook Air) — required for persistent env / production Supabase access
- 6 disposable test accounts: `gh-test-player-N@test.guess-history.com`, recreated by `globalSetup()` every run

### Harness-Itself Defects (MP-PLAN-TESTHARNESS-FULL-FIX-001 / MP-EXEC-TESTHARNESS-FULL-FIX-001)

Defects found in the harness itself (not app bugs). Plan tracked in `CTO_BACKLOG.md` under this section.

| ID | Defect | Status | Commit | Task |
|----|--------|--------|--------|------|
| H1 | PLAY_AGAIN wait is no-op setTimeout(2000); handler never attached | CLOSED | fe58ac6 | T2 |
| H2 | Dead `roster` locator in observer.ts (created, never used) | CLOSED | 0bad7f1 | T1 |
| H3 | readyForNext in ROUND_COMPLETE is placeholder, never asserted | CLOSED | cbe7966 | T10 |
| H4 | hasSubmitted depends on CSS class name, never asserted | CLOSED | cbe7966 | T10 |
| H5 | observeState single-read no retry → DOM/WS race | CLOSED | 0bad7f1 | T1 |
| H6 | late-join edge case is no-op (logs "skipping") | CLOSED | 499624b | T5a |
| H7 | timeout edge case does nothing (no submission skipped) | CLOSED | 3dcb326 | T5c |
| H8 | 7th-player-join never asserts rejection | CLOSED | 07111b1 | T5b |
| H9 | only-one-next undone by orchestrator's readyNext loop | CLOSED | f0ef378 | T5d |
| H10 | 3 refresh edge cases log diffs but never fail test | CLOSED | 2d7113a | T3 |
| H11 | 4 auth edge cases log verdict but never fail test | CLOSED | 2d7113a | T3 |
| H12 | EdgeCaseEngine swallows inject errors | CLOSED | 2d7113a | T3 |
| H13 | Spec assertions don't check edge case outcomes | CLOSED | 7827d19 | T4 |
| H14 | No pre-flight load check (load <10 rule human-only) | CLOSED | aacd954 | T6 |
| H15 | Edge cases only run in game 1 of 3 (injected Set persists) | CLOSED | 59b4cc4 | T9 |
| H16 | refresh() no auth-modal-never-detaches diagnostics | CLOSED | ef366a3 | T7 |
| H17 | submitGuess fire-and-forget, rejection undetected | CLOSED | b8e48d4 | T8a/T8b |

### Other Active Multiplayer Issues
| Issue | Status | Notes |
|-------|--------|-------|
| Sign-in/sign-out intermittency (sign-out sometimes UI-only, session not cleared) | CLOSED (MP-FIX-AUTH-SIGNOUT-CHUNKED-COOKIE-001) | Root cause confirmed via MP-INV-AUTH-SIGNOUT-INTERMITTENCY-001/-VERIFY (GH-NEW2): identity.ts signOut()'s failure-path cookie clear only matched exact sb-*-auth-token name, missing chunked cookies (.0/.1, created when session >3180 bytes) and the PKCE code-verifier cookie — @supabase/ssr combineChunks then reconstructed a valid session from leftover chunks. Fix: signOut() now calls the existing forceClearAuthStorage() (clears ALL sb-* cookies + localStorage) unconditionally in the finally block, regardless of whether auth.signOut() succeeds, fails, or is aborted by the caller's own navigation. One file changed: src/core/identity.ts. Live-verified against local dev (chunked-cookie abort-path + happy-path regression, both PASS — 0 sb-* cookies remaining, correct /login redirect on nav and hard reload in all cases). **Deferred KC-009 manual smoke test completed** under MP-VERIFY-SYNC-GOLDEN-PATH-CLEANRUN-002: host signed out in a two-context sync compete lobby; guest session and connection were unaffected, both rosters remained at 2 players. Commit 7db2363, PR #26, merged a18acedf1f15713609dbd7329e9a5b04e4f12d3a. |
| 5-player live test disconnect at RESULT_PHASE (2026-06-21) | OPEN | One player dropped, cause undiagnosed. Needs read-only investigation. Fold into MP-AUTO-TEST-001 scenario inventory as "disconnect-at-RESULT_PHASE recovery" — but root cause of THIS incident is separate from the general reconnect-resilience scenario; do not conflate. Product goal: not "never disconnects" but "always recoverable, zero data loss, clear path back in." |
| Home Compete card "Your turn" indicator + cold re-entry | SUPERSEDED by B9 | Original scope was unclear on session types and approach. See Section B row B9 for current scope (Rush + Relax both in scope, row/link inside Your Turn tab not forced tab-switch). |
| MP-FIX-VIEWER-BROADCAST-001 (guest doesn't see host's live era/year-range changes in lobby) | CONFIRMED BROKEN (2026-06-18) | Separately PARKED/deprioritized — broken and parked are both true, not contradictory. Not urgent. |

---

## SECTION B — UIX BACKLOG (opened 2026-06-24)

**Priority note:** This entire section is a parallel track to MP-AUTO-TEST-001. The harness has priority for Devin Local if both need it at the same time — Devin Local is the only coder execution environment in use (Devin Cloud is not used on this project); Windsurf/Cascade handles commits/merges as usual.

| # | Item | Status | Notes |
|---|------|--------|-------|
| B1 | Home > Compete card invite link sometimes unresponsive until browser refresh | CLOSED (MP-FIX-INVITE-TIMEOUT-CLIENT-001, commit 198437c — 8s timeout on auth.getUser() in getValidAccessToken; MP-FIX-INVITE-TIMEOUT-CLIENT-002, commit 53d8b1d — corrected incomplete fix by returning null immediately on getUser() timeout to avoid getSession() lock deadlock; MP-FIX-INVITE-TIMEOUT-SERVER-001, commit ed04088 — 8s timeout on /api/invitations/pending route) | Phase B-001: 4 scenarios tested (3 FAIL, 1 code-read). Phase B-002 (MP-INV-INVITE-LINK-UNRESPONSIVE-B-002): dev server started (I started it), live Playwright verification run. INV-B1-01 PASS: hanging /api/compete/create leaves Create Game and Join Game buttons disabled after 60s (BEFORE: disabled=false, AFTER: disabled=true). INV-B1-02 PASS: hanging /auth/v1/user intercepted successfully, spinner visibility check completed (output truncated). INV-B1-03 SKIPPED: no test invite row exists - test fixtures do not support inserting one into game_invitations. Proposal: Use Supabase MCP to insert a test invite row before running this test. Test file: scripts/test/playwright/specs/invite-hang-scenarios.spec.ts. Commit: pending. Phase B-005 (MP-INV-INVITE-LINK-UNRESPONSIVE-B-005): PREMISE CHANGED — fetchInvites now calls BOTH getValidAccessToken()→auth.getUser() (original hang point, still present) AND fetch('/api/invitations/pending') (new endpoint, also unguarded await with no timeout). Two hang points identified: (1) await getValidAccessToken() → await supabaseBrowser.auth.getUser() at supabaseBrowser.ts:27; (2) await fetch('/api/invitations/pending', {headers}) at CompetePanel.tsx:64. Both lack timeout; if either hangs indefinitely, invitesLoading stays true forever (catch block only fires on rejection, not on infinite pending). No reusable browser storageState found in repo (auth fixture creates users via admin API but does not save storageState; auth-ui.ts logs in via UI each run). STOPPED at Step One per task instructions — awaiting CTO sign-off on revised repro target before proceeding. B-003/B-004 superseded (B-003: wrong method — Playwright spec in other coder's dir; B-004: infeasible manual DevTools instruction). Phase B-006 (MP-INV-INVITE-LINK-UNRESPONSIVE-B-006): CODE-CONFIRMED: no timeout protection exists on either hang point. (1) Client-side: getValidAccessToken() at supabaseBrowser.ts:26-29 calls await supabaseBrowser.auth.getUser() then await supabaseBrowser.auth.getSession() — no AbortController, no Promise.race timeout, no client-side timeout configured on createBrowserClient (supabaseBrowser.ts:16-19 — only URL and anon key passed, no fetch timeout option). (2) Server-side: /api/invitations/pending/route.ts:6-63 — the GET handler calls await supabase.auth.getUser() (line 23) and await supabase.from('game_invitations').select(...).eq('invitee_id', user.id)... (lines 30-37) with no AbortController, no statement_timeout, no Promise.race, no route-level timeout wrapper. The createClient call at route.ts:13-21 passes only global headers, no fetch timeout. DB query is indexed: idx_game_invitations_invitee ON (invitee_id, status) covers the .eq('invitee_id') + .eq('status') + .gt('expires_at') + .order('created_at') + .limit(5) query — single indexed lookup, unlikely to be slow under normal load. However, the N+1 profile lookups (Promise.all of .single() per invite, lines 43-49) add up to 5 additional round-trips with no timeout. The profiles.id lookup is a PK scan (indexed). Conclusion: both hang points confirmed by direct code reading — no live repro needed (Step Two decision gate sufficient). Root cause: absence of timeout protection on both client-side auth.getUser() and server-side fetch chain, not a slow query. Phase E (MP-VERIFY-INVITE-TIMEOUT-E-001): CLIENT-SIDE LIVE VERIFICATION — FAIL. Method: Playwright script (scratch, deleted post-run) logged in via UI, navigated to /leaderboard (client-side), set up page.route('**/auth/v1/user*') to block auth.getUser() indefinitely, then clicked back button (client-side router.back()) to return to /. bootstrapIdentity returned cached state (playerId set), CompetePanel remounted with invitesLoading=true, fetchInvites called getValidAccessToken. Results: auth/v1/user call blocked at t≈540ms after back navigation. Console warning "getValidAccessToken: auth.getUser() timed out after 8s" appeared at t=8085ms — the 8s Promise.race timeout fired correctly. HOWEVER, /api/invitations/pending was NEVER requested (0 requests in 15s observation window) — fetchInvites never completed. Root cause: auth.getSession() at supabaseBrowser.ts:35 hangs because Supabase's GoTrueClient uses an internal lock (_acquireLock at GoTrueClient.js:2249-2301). getUser() acquires the lock (lockAcquired=true, line 2273) and never releases it because the blocked network request never resolves. getSession() sees lockAcquired=true (line 2252), enters pendingInLock queue, awaits the never-resolving getUser() promise (line 2257) — deadlocks. The Promise.race timeout fires and rejects, but the catch handler only logs the warning; getSession() on the next line still blocks on the lock. VERDICT: FAIL — the client-side fix (commit 198437c) is INCOMPLETE. The timeout fires but does not unblock getValidAccessToken because getSession() deadlocks on the Supabase internal lock held by the still-pending getUser(). B1 REOPENED. Server-side fix (commit ed04088) accepted on static evidence — not tested live. Scratch script deleted, no files left in scripts/test/_scratch/. Phase E (MP-VERIFY-INVITE-TIMEOUT-E-002): CLIENT-SIDE RE-VERIFICATION — PASS. Method: Same scenario as E-001 (Playwright scratch script, deleted post-run). Results: auth/v1/user call blocked at t≈239ms after back navigation. Console warning "getValidAccessToken: auth.getUser() timed out after 8s" appeared at t=8239ms — timeout fired correctly. /api/invitations/pending request fired at t=8239ms (immediately after timeout) with auth header ABSENT — fetchInvites completed. Spinner snapshots: t=10s: spinner=false (count=0), t=15s: spinner=false (count=0). Primary evidence: /api/invitations/pending network request fired post-timeout (did NOT fire in E-001), proving fetchInvites unblocked after getValidAccessToken returned null. VERDICT: PASS — the corrected fix (commit 53d8b1d) successfully unblocks fetchInvites by returning null immediately on getUser() timeout, avoiding the getSession() lock deadlock. B1 CLOSED. Scratch script deleted, no files left in scripts/test/_scratch/. |
| B2 | In-game map search can't resolve some English place names (confirmed failing: Delhi, Vietnam, Thailand) | CLOSED (MP-FIX-MAP-SEARCH-B2-001, commit de04fb4) | Root cause: client-side direct cross-origin fetch to Nominatim was being blocked (CSP/CORS). Server-side proxy route at /api/geocode/search existed but was dead code. Fix: consolidated search to use the existing server proxy route. Nominatim itself returns correct results for all three queries (confirmed via curl). |
| B3 | **PRIORITY** — Rating button (1–10) at round/final results | CLOSED (MP-INV-FIX-RATING-B3-001) — round result screen only | Investigation verdict: state (c) NOT BUILT AT ALL — no rating table, no API, no UI, no History Collection rating storage. Built new: migration `20260625000000_create_player_event_ratings.sql` (table `player_event_ratings(player_id,event_id,rating 1-10,rated_at, PK player_id+event_id)` + RLS owner select/insert/update); API route `src/app/api/ratings/route.ts` (POST upsert + GET one-event); component `src/components/compete/RatingControl.tsx` (1-10 tappable scale, fetches existing rating, persists via POST); wired into `RoundCompleteSection.tsx` event card next to Source button. Scope = round result screen ONLY (final-screen + History Collection rating UI out of scope per CTO lock; storage shape is the canonical one History Collection will reuse). tsc clean (only pre-existing unrelated errors in rules.correctness.test.ts). FOLLOW-UP (MP-VERIFY-FIX-BATCH-B6-B9-001): live-DB audit via direct pg query found `player_event_ratings` table does NOT exist in production Supabase — migration `20260625000000` was committed to repo but NEVER APPLIED to live DB (latest applied in `supabase_migrations.schema_migrations` is `20260611092811`; `profiles.locale` from `20260625000001` likewise absent). FK gap is therefore moot — no table to add FK to. Status kept CLOSED per task instruction; CTO decision required on applying the pending batch migrations before any FK work. SUPERSEDED (MP-FIX-APPLY-RATINGS-LOCALE-MIGRATIONS-001): CTO-ratified schema correction applied LIVE. New migration `20260625000002_create_player_event_ratings_corrected.sql` (original `20260625000000` left untouched, both recorded applied in `supabase_migrations.schema_migrations`) creates `player_event_ratings` with the CTO corrections: (1) `event_id` FK to `events(id)` ON DELETE CASCADE (`player_event_ratings_event_id_fkey` confirmed live), (2) added `updated_at TIMESTAMPTZ NOT NULL DEFAULT now()`. API route `src/app/api/ratings/route.ts` upsert fixed: `rated_at` omitted from payload (defaults on INSERT, preserved on re-rate via ON CONFLICT DO UPDATE excluding it), `updated_at` sent on every write. LIVE-VERIFIED end-to-end via direct pg + authenticated HTTP against localhost:3000: POST rating=7 → GET 7 → POST rating=9 → GET 9, row count stays 1, `rated_at` UNCHANGED across re-rate, `updated_at` CHANGED, no-cookie GET → 401. |
| B4 | Language choice at sign-in/sign-up | CLOSED (MP-FEAT-AUTH-LANG-B4-001) | Schema gap found: `profiles` had no locale column. Added migration `20260625000001_add_locale_to_profiles.sql` (nullable `locale TEXT`, no default — trivially safe/reversible via DROP COLUMN). Wired existing `LanguageSwitcher.tsx` (was dead code, never rendered) into `AuthModal.tsx` under the title. Modified `src/actions/setLocale.ts` to persist the locale to `profiles.locale` when the user is signed in (cookie always set for immediate runtime effect; profile write is best-effort and non-fatal). NavModal.tsx NOT touched (its own parallel locale impl is a known future cleanup, out of scope). tsc clean (only pre-existing unrelated errors in rules.correctness.test.ts). SUPERSEDED (MP-FIX-APPLY-RATINGS-LOCALE-MIGRATIONS-001): migration `20260625000001` was committed but NEVER APPLIED to live DB (confirmed via direct pg: `profiles.locale` absent, latest applied migration was `20260611092811`). Now applied LIVE via direct pg execution (MCP `apply_migration` unavailable) and recorded in `supabase_migrations.schema_migrations`. LIVE-VERIFIED: `information_schema.columns` confirms `profiles.locale` is `text`, nullable, no default; round-trip persistence verified via direct query (set 'fr'→read 'fr', set 'en'→read 'en', set NULL→read NULL). Status stays CLOSED. |
| B5 | Image zoom during active round goes toward center instead of toward touch/pinch point | CLOSED — MP-FIX-UI-ZOOM-B5-001, commit `c835953` | Pinch handler in `RoundActiveSection.tsx` now computes the pinch midpoint relative to the container, reads the image's current rendered rect via `getBoundingClientRect()`, and adjusts `panX`/`panY` so the content point under the midpoint stays fixed after the scale change. The `transform-origin: 50% 50%` (default) is accounted for in the anchor math. |
| B6 | Welcome modal should let new users pick an avatar using the same picker UI as the profile page avatar picker | CLOSED (MP-FIX-UI-AVATAR-WELCOME-B6-001) | Profile page avatar picker was inline JSX (no reusable component). Extracted shared `src/components/AvatarPickerModal.tsx` (props: isOpen, currentAvatarUrl, onSave, onClose, showSkip, onSkip) reusing `avatarPicker.module.css`. Used in both `src/app/profile/page.tsx` and `src/components/WelcomeModal.tsx`. In Welcome modal the picker is skippable (showSkip=true); a "Choose a different avatar" button opens it. tsc clean (only pre-existing unrelated errors in rules.correctness.test.ts). |
| B7 | Profile page avatar has no edit-icon visual cue — clicking the avatar opens the editor but nothing signals it's clickable | CLOSED (MP-FIX-UI-AVATAR-EDITICON-B7-001) | Added a pencil/edit-icon overlay (absolute-positioned bottom-right badge, orange bg, pencil SVG) on the profile page avatar circle (`src/app/profile/page.tsx`). Pure visual affordance via new `.avatarEditIcon` class in `profile.module.css` (pointer-events:none so the existing click handler is unchanged). B6's shared component is the picker MODAL; B7 targets the avatar DISPLAY circle, which is separate — no prop addition needed. tsc clean (only pre-existing unrelated errors in rules.correctness.test.ts). |
| B8 | Low priority (raise if simple/quick): WHERE/WHEN result popup cards — two fixes: (1) remove/disable the bottom-sheet draggable-handle behavior entirely — sheet must only close via a clearly visible close button, never by drag; (2) any input field inside these popups must have the cursor positioned inside it by default (autofocus) — since this shifts content upward, the map section must be resized/repositioned to exactly cover the entire upper part of the screen when this happens | CLOSED (MP-INV-FIX-SHEET-MAP-DEFAULT-B8-001, commit a78738a) | CTO-confirmed target was the GUESS_PHASE bottom-sheet in `src/components/compete/RoundActiveSection.tsx` (NOT the RESULT_PHASE WhereCard/WhenCard cards — those remain out of scope). Fix: WHERE/map sheet now opens FULLY EXPANDED by default every time (always applies `.sheetFull`); removed `sheetExpanded`/`sheetDrag` state, the three drag-gesture handlers (`onSheetHandleDown/Move/Up`), the shared drag-handle JSX (`.dragZone`/`.dragHandle`) and the `--sheet-drag` CSS var from `RoundActiveSection.module.css`. Sheet closes only via the explicit close button (`sheetCloseBtn`), confirm button, or backdrop tap — never via drag. WHEN sheet behavior unchanged (fixed height via `.sheetWhen`); its shared drag-to-close was removed with the handle but it retains its close/confirm buttons + backdrop. GameMap props do not depend on the removed state. Part (2) (autofocus + map resize) was NOT in this task's scope and remains unaddressed. tsc clean (only pre-existing unrelated errors in rules.correctness.test.ts). |
| B9 | MEDIUM PRIORITY: When a Compete game is in progress (either Rush sync, round still active and player hasn't submitted, OR Relax async, player hasn't submitted), show the game link/row in Home > Compete card > "Your turn" tab so the user can tap it to re-join and continue playing | CLOSED (MP-FEAT-HOME-YOURTURN-B9-001) | Investigation: cold re-entry ALREADY WORKS — DO `loadFromDB()` → `loadCompeteSessionSnapshot()` → `getGameState()` → STATE_UPDATE broadcast; `/compete/[gameId]` renders by `snapshot.status` without forcing a fresh lobby. CompetePanel already had the `your_turn` tab + badge count + "Play" badge + `onLobby(game.game_id)` navigation. Gap: `active-games` route (line 119) only returned `"your_turn"` for async (sync always `"waiting"` even when round active + not submitted) — this was the prior async-only decision, now REVOKED. Fix: removed the `&& session.mode === 'async'` restriction so sync (Rush) games with an active unsubmitted round also surface as `your_turn`; added `mode` ('sync'|'async') to the route response and the `ActiveGame` type; added a Rush/Relax mode badge on your-turn rows (`CompetePanel.tsx` + `CompetePanel.module.css` + i18n keys `compete_mode_rush`/`compete_mode_relax`). No forced tab-switch. No new multiplayer stack plumbing. tsc clean (only pre-existing unrelated errors in rules.correctness.test.ts). |
| B10 | Practice mode stuck permanently on "Loading game" after clicking Start Practice | DONE (BUG-FIX-PRACTICE-LOAD-001, commit 808221d) | Root cause (BUG-INV-PRACTICE-LOAD-001): `/practice/[gameId]/page.tsx` loading gate `identityLoading \|\| identityError \|\| !snapshot` hung forever — the 10s escape hatch present on /home, /practice, /compete, /daily was NOT added here, and the snapshot fetch (`GET /api/compete/[gameId]`) had no AbortController timeout. Fix: (1) added `showLoadingTimeout` state + 10s mount timer useEffect surfacing a "Taking too long? Clear session & return home" button calling `forceClearAuthStorage()` (imported from `@/core/identity`); (2) wrapped snapshot fetch in 15s AbortController — on abort sets error "Session load timed out. Please try again." One file modified: `src/app/practice/[gameId]/page.tsx`. tsc clean (exit 0); next lint exit 0 (warnings only, all pre-existing); pre-commit KC-007 guard + 45 tests passed. NOTE: a pre-existing uncommitted change to `src/server/sessionCore.ts` (advanceRound early-closure rule) was present in the working tree and intentionally NOT included in this commit. |
| B11 | Compete (sync/async) session jumps to SESSION_COMPLETE after round 1 instead of advancing to round 2 | DONE (BUG-FIX-COMPETE-EARLY-CLOSURE-001, commit c6de4ed) | Root cause (BUG-INV-COMPETE-GAMELOOP-001): `advanceRound()` in `src/server/sessionCore.ts` early-closure rule `shouldCloseEarly = !isSoloMode && nonLeftCount < 2` fired for any compete session with fewer than 2 active players — including single-player sessions that only ever had 1 player (not a player leaving mid-game). Fix: added `totalEverJoined = activePlayerRows.length` (all rows incl. left) and gated the rule: `shouldCloseEarly = !isSoloMode && totalEverJoined >= 2 && nonLeftCount < 2`. The early-closure rule now fires ONLY when at least 2 players ever joined AND fewer than 2 remain (i.e. a player left mid-game). One file modified: `src/server/sessionCore.ts` (lines 2622-2624). tsc clean (exit 0); next lint exit 0 (warnings only); pre-commit KC-007 guard + 45 tests passed. |
| B12 | Sync compete: round timer hidden behind WHERE/WHEN bottom sheet during active round | DONE (BUG-FIX-SYNC-POLISH-001, commit pending) | Root cause: `.timerWrapper` (z-index 15) is a sibling of `.bottomPanel` (z-index 20) inside `.section`. The bottom panel creates a stacking context; its children (`.sheetBackdrop` z-index 40, `.sheet` z-index 41, `.sheetFull` height 100dvh) all paint above the timer because the entire `.bottomPanel` subtree participates at z-index 20 from `.section`'s perspective. Fix: raised `.timerWrapper` z-index from 15 to 42 in `src/components/compete/RoundActiveSection.module.css` (line 146). Value 42 is above the sheet's internal z-index (41) but below `.mapFullscreenOverlay` (50) and `.settingsOverlay` (10000). `.sheetFieldWrap` (KC-001, z-index 1001) was NOT touched — confirmed by `git diff` grep. Also fixed KC-001 regression guard grep in `docs/KNOWN_CONSTRAINTS.md` from `grep -n "sheetFieldWrap" ... \| grep "z-index: 1001"` (returns empty due to multi-line CSS) to `grep -A4 "sheetFieldWrap" ... \| grep "z-index: 1001"` (returns match). tsc clean (exit 0); next lint exit 0 (warnings only, all pre-existing); KC-001 guard PASS. |
| B13 | Sync compete: avatar + username missing from auto-advance/ready section on round results (reclassified: feature addition, not a regression) | DONE (BUG-FIX-SYNC-POLISH-001, commit pending) | Git investigation (`git log --all -S "avatarUrl" -- src/components/compete/RoundCompleteSection.tsx` → only commit 11eaa2a, file creation) confirmed avatars were NEVER in the `readyForNext` section in any committed version — `PlayerAvatar` was only used in leaderboard rows (lines 412, 434). The original `readyForNext` rendering (commit 11eaa2a, line 354-361) used `name ✓` gradient text with no avatar, identical to the current code. This is a feature addition, not a regression. Fix: added `<PlayerAvatar avatarUrl={p?.avatarUrl ?? null} displayName={name} size={24} />` to each `readyName` span in `src/components/compete/RoundCompleteSection.tsx` (lines 602-613), and added `display: inline-flex; align-items: center; gap: 4px;` to `.readyName` in `src/components/compete/RoundCompleteSection.module.css` (line 684). `PlayerAvatar` was already imported (line 6). tsc clean (exit 0); next lint exit 0 (warnings only, all pre-existing). |
| B14 | Sync compete: refresh on round results shows 0% accuracy (investigation only, NOT fixed in this task) | INVESTIGATING (diagnostic conclusion: environment/timing-dependent, may still intermittently reproduce) | Reconciliation: no commits since investigation changed `RoundCompleteSection.tsx` or `useCompeteSocket.ts` (last: e68e062). Code unchanged. Analysis: on reconnect, if the PartyKit DO is alive, `roundResultsForClient` persists on the in-memory snapshot (set at `partykit/server.ts:1345` during submitGuess, applied to `this.snapshot` via `applySnapshotAndBroadcast` at line 505) and results arrive via WS STATE_UPDATE immediately (handler at `useCompeteSocket.ts:73-84` fires `onRoundResults`). If the DO was evicted (cold load via `loadFromDB` at line 379), `roundResultsForClient` is NOT set (loadFromDB does not call `getRoundResults`) and `results` is `undefined` in the STATE_UPDATE — the REST fetch at `useCompeteSocket.ts:125-149` is the fallback. During the REST fetch window, `roundResults` is `null` → `accuracy = 0` → RainbowRing shows its animation starting point (0). The ring always animates from 0 to the target value, so the brief 0% is the animation origin, not a stuck value. The fetch-failure path (`onRoundResults([])` at lines 141/146) would cause a permanent 0%, but that's a network-error scenario. Live test showing correct accuracy likely hit the common case (DO alive). The original report may have hit the rare case (DO evicted) or observed the ring before the REST fetch completed. Status: NOT fixed — no code change made. May still intermittently reproduce under DO eviction + slow/failed REST fetch. |

---

## SECTION C — TOP-LEVEL PRIORITIES (set 2026-06-18)

> ⚠️ **REVERT-BEFORE-LAUNCH ITEM (TEMPORARY)** — MP-FIX-COMPETE-SOLO-START-TEMP-001 (commit `8cfc42c`, 2026-06-27):
> Min players lowered to 1 for solo testing. **MUST be reverted to 2 before any multi-player/production testing or launch.**
> Changed locations:
> - `partykit/server.ts:227` — `MIN_PLAYERS_TO_START` constant `2` → `1` (the only remaining hardcoded start-gating threshold; all other start-gating sites — `src/server/sessionCore.ts:461` allPlayersReady derivation, `src/server/sessionCore.ts:1139` startCompeteSession validation, `partykit/server.ts:765` broadcastStateUpdate allPlayersReady — were already at `1` from prior fix MP-FIX-START-MIN-PLAYERS-001).
> - The constant is consumed at `partykit/server.ts:445` (attemptAutoStart allReady check), which inherits the change.
> Note: this same fix will need `npx partykit deploy` to take effect in production (local `partykit dev` hot-reloads).

1. MP-AUTO-TEST-001 — automated 6-concurrent-user test harness (Section A)
2. Gameplay image zoom during active round (relates to B5 above but is the original, broader priority item)

---

## CLOSED LOG

- **MP-IMPL-MAP-LANG-001** (2026-06-19): CARTO Voyager tiles for English map labels. Commit `c0a8636`. Visually confirmed. RISK ACCEPTED: CARTO free tier is non-commercial-only — must move to paid plan before commercial launch (pre-launch blocker, tracked, not "someday"). Future scope, not started: local/English toggle (Zustand + Supabase + Settings UI).
- **Avatar/profile backfill** (2026-06-18): 67/111 accounts missing `profiles` row, backfilled via direct Supabase MCP. 111/111 confirmed. Avatar picker bug was a data gap, not a code bug — closed.
- **MP-FEAT-BOTTOMBAR-RESTYLE-001** (2026-06-19): Countdown card, Next button, progress dots restyled. Visually confirmed.
- **RainbowRing.tsx styling** (2026-06-19, commit `6386583`): Track stroke + center text. Color logic FINAL: `hsl(round((clamped/100)*120), 100%, 50%)` — value-based red→green hue. Gradient version explicitly REJECTED — do not reintroduce.
- **MP-REFACTOR-CARD-TOKENS-001**: Card design tokens into `globals.css`. Commit `85303be`. Done.
- **MP-ADD-WAITLIST-TBL-001** (2026-06-26): Created `public.waitlist` table (id UUID PK default gen_random_uuid(), email TEXT NOT NULL, created_at TIMESTAMPTZ default now(), UNIQUE(email)) with RLS enabled and no authenticated policies (implicit deny; service role bypasses). Migration applied via Supabase MCP (schema_migrations version `20260626111106` `create_waitlist`); file `supabase/migrations/20260626000000_create_waitlist.sql`. Validated: \d waitlist shows columns + `waitlist_email_unique` UNIQUE; duplicate insert rejected; RLS `relrowsecurity=t`; exactly one migration file. Commit `ebb346d`.
- **MP-MOVE-HOME-PAGE-001** (2026-06-26): Pure relocation — copied `src/app/page.tsx` → `src/app/home/page.tsx` byte-identical. `src/app/page.tsx` left untouched (will be overwritten by later MP-ADD-LANDING-PAGE-001). `diff` empty; `git diff --name-only` shows only the new file. tsc introduces zero new errors (2 pre-existing errors in `rules.correctness.test.ts` are unrelated and pre-existed on prior commit). Commit `872c2aa`. **KNOWN ISSUE:** `import styles from './home.module.css'` in the copy resolves to `src/app/home/home.module.css` (doesn't exist; CSS is at `src/app/home.module.css`) — `next build` fails with Module not found. Needs separate fix task (CSS path relocation or copy).
- **MP-FIX-HOME-STRIP-GATING-001** (2026-06-26): Removed all client-side auth-gating from `src/app/home/page.tsx` — deleted `showAuthModal` state, `AuthModal` import + render, all `setShowAuthModal` calls in identity callbacks, `handleNav` early-return guard, `onAvatarClick` identity check; replaced `onRequireAuth` with no-op (ModeCard component definition untouched). Preserved `bootstrapIdentity`, `subscribeToIdentityChanges`, isNewUser avatar-fetch, all stats/profile fetching. `grep showAuthModal` → 0; `grep AuthModal` → 0; only `src/app/home/page.tsx` modified; `src/app/page.tsx` unchanged. tsc: no new errors (2 pre-existing in `rules.correctness.test.ts`). `next build` fails due to pre-existing CSS path issue from MP-MOVE-HOME-PAGE-001 (not caused by this task). Commit `f5f582f`.
- **MP-UPD-MIDDLEWARE-HOME-AUTH-001** (2026-06-26): **STOPPED — CRITICAL PRE-EXISTING ISSUE.** Middleware logic in `middleware.ts` is correct (PUBLIC_PATHS has `/` but not `/home`; `if (!user)` redirects to `/login?next=/home`) — NO code change was needed. HOWEVER, the middleware is NOT being loaded by Next.js at all: `.next/server/middleware-manifest.json` is empty (`"middleware": {}`). Root cause: project uses `src/` directory, so Next.js 14 requires middleware at `src/middleware.ts`; the current `middleware.ts` at project root is IGNORED. Proof: temporarily copied to `src/middleware.ts`, dev server compiled it, and `curl -i http://localhost:3000/home` returned `307 → /login?next=%2Fhome` (correct). Reverted. **IMPACT: ALL routes are currently unprotected — middleware has never been running. Client-side gating was the ONLY auth gate (now removed from /home by MP-FIX-HOME-STRIP-GATING-001).** Required fix (separate task): move `middleware.ts` → `src/middleware.ts`. Also blocking: `src/app/home/page.tsx` CSS import `./home.module.css` is broken (pre-existing from MP-MOVE-HOME-PAGE-001). No commit for this task (no code change). PROGRESS.md updated with commit `ecab490`.
- **MP-ADD-WAITLIST-API-001** (2026-06-26): Created `src/app/api/waitlist/route.ts` — POST-only endpoint for waitlist email submission. Validates email via regex, uses service-role Supabase client (`createSupabaseServerClient`), returns 201/409/400/405/500 per contract. Live-verified via curl: 201 on valid insert, 409 on duplicate, 400 on invalid email, 405 on GET. Test row cleaned up. tsc: no new errors (2 pre-existing in `rules.correctness.test.ts`). Commit `ac73135`.
- **MP-UPD-LOBBY-HOME-001** (2026-06-26): Single-line change in `src/components/compete/LobbySection.tsx` line 431 — `router.push("/")` → `router.push("/home")`. Back button now navigates to /home instead of landing page. grep verified: 0 old matches, 1 new match. tsc: no new errors. Commit `9e0e254`.
- **MP-UPD-ROUNDCOMP-HOME-001** (2026-06-26): Single-line change in `src/components/compete/RoundCompleteSection.tsx` line 447 — `router.push("/")` → `router.push("/home")`. Home button now navigates to /home instead of landing page. grep verified: 0 old matches, 1 new match. tsc: no new errors. Commit `31e1a2a`.
- **MP-UPD-SESSIONCOMP-HOME-001** (2026-06-26): Single-line change in `src/components/compete/SessionComplete.tsx` line 394 — `router.push("/")` → `router.push("/home")`. Home button now navigates to /home instead of landing page. grep verified: 0 old matches, 1 new match. tsc: no new errors. Commit `380c62a`.
- **MP-UPD-COMPETE-HOME-001** (2026-06-26): Single-line change in `src/app/compete/page.tsx` line 20 — `router.push("/")` → `router.push("/home")`. Compete page redirect now goes to /home instead of landing page. grep verified: 0 old matches, 1 new match. tsc: no new errors. Commit `e3fe2fc`.
- **MP-UPD-ACCOUNT-SIGNOUT-CONFIRM-001** (2026-06-26): **Confirmation-only, no code change.** Verified `src/app/account/page.tsx` line 129 reads `router.push('/')` after `await signOut()` — already correct per CTO decision (post-signOut lands on "/" public landing page, NOT "/home" which would bounce to /login via middleware). No correction needed. tsc: no new errors. PROGRESS.md updated with commit `78f6d34`.
- **MP-UPD-PROFILE-SIGNOUT-CONFIRM-001** (2026-06-26): **Confirmation-only, no code change.** Verified `src/app/profile/page.tsx` line 194 reads `router.push('/');` after `await signOut();` — already correct per CTO decision (post-signOut lands on "/" public landing page, NOT "/home"). No correction needed. tsc: no new errors. PROGRESS.md updated with commit `c114267`.
- **MP-UPD-AUTHMODAL-HOME-001** (2026-06-26): Single-line change in `src/components/AuthModal.tsx` line 51 — OAuth `redirectTo` `next=/` → `next=/home`. Post-OAuth-success user has a session, so /home is correct (middleware permits). Line 124 (`next=/account` for account-linking) untouched. grep verified: no bare `next=/` remains. tsc: no new errors. Commit `33d70da`.
- **MP-ADD-LANDING-PAGE-001** (2026-06-26): Replaced `src/app/page.tsx` with new SEO-optimized public landing page (server component, Next.js Metadata API with title/description/OG tags, semantic HTML, existing CSS variables + gradient conventions). Created `src/components/landing/WaitlistForm.tsx` (client component) — POSTs to /api/waitlist with inline states (idle/submitting/success/already/error). curl / → 200 with title, OG tags, email input. Form flow verified: 201 on fresh email, 409 on duplicate. Test row cleaned up. `next build` fails due to pre-existing CSS path issue in `src/app/home/page.tsx` (from MP-MOVE-HOME-PAGE-001, not caused by this task). tsc: no new errors. Commit `b26ebb2`.

### WAITLIST LANDING PAGE INITIATIVE — E2E VERIFICATION (MP-VERIFY-WAITLIST-E2E-001, 2026-06-26)
**Status: CLOSED — confirmed by direct manual verification (Lolo), 2026-06-27.**

Tasks in this initiative (final task chain):
- MP-ADD-WAITLIST-TBL-001
- MP-MOVE-HOME-PAGE-001
- MP-FIX-HOME-STRIP-GATING-001
- MP-UPD-MIDDLEWARE-HOME-AUTH-001
- MP-ADD-MIDDLEWARE-BYPASS-001 (blocked, never shipped)
- MP-ADD-WAITLIST-API-001
- MP-UPD-LOBBY-HOME-001
- MP-UPD-ROUNDCOMP-HOME-001
- MP-UPD-SESSIONCOMP-HOME-001
- MP-UPD-COMPETE-HOME-001
- MP-UPD-ACCOUNT-SIGNOUT-CONFIRM-001
- MP-UPD-PROFILE-SIGNOUT-CONFIRM-001
- MP-UPD-AUTHMODAL-HOME-001
- MP-ADD-LANDING-PAGE-001
- MP-VERIFY-WAITLIST-E2E-001 (first run, 8/15)
- MP-FIX-HOME-CSS-IMPORT-001
- MP-FIX-MIDDLEWARE-LOCATION-001
- MP-VERIFY-WAITLIST-E2E-001 (re-run, 10/15)
- MP-FIX-WAITLIST-PUBLIC-ROUTE-001
- MP-ADD-MIDDLEWARE-BYPASS-002
- MP-FIX-WAITLIST-LINT-001
- MP-FIX-LANDING-VISUALS-001
- MP-FIX-LANDING-LOGO-CONTRAST-001 (initially incorrect "no bug" finding on logo)
- MP-INV-LOGO-RENDER-MISMATCH-001 (found real root cause)
- MP-FIX-MIDDLEWARE-STATIC-ASSETS-001 (actual fix)

**Lessons learned:**
- A file move that is byte-identical in content can still break at runtime if it has location-dependent behavior (relative imports, platform-specific file placement conventions like Next.js middleware location).
- DOM load-state properties (complete, visibility, opacity) on an <img> do not prove valid image content — only that the element finished a load attempt. Pixel-level verification (actual decoded content, not just element state) is required to close out a "is this image broken" question.
- Middleware path-allowlist changes need a static-asset smoke test, not just route-level checks — this gap let public/ assets stay broken for two full verification rounds before being caught by manual screenshot inspection, not automated checks.

E2E verification results — RE-RUN after MP-FIX-HOME-CSS-IMPORT-001 + MP-FIX-MIDDLEWARE-LOCATION-001 (15 checks):
1. PASS — grep compete-area router.push("/") → 0 matches
2. PASS — grep AuthModal next=/ bare → 0 matches
3. PASS — account + profile signOut targets both show '/'
4. PASS — PUBLIC_PATHS (in src/middleware.ts) has "/", absent "/home"
5. PASS — curl / → 200, landing page HTML
6. PASS — curl /home (no cookies) → 307 to /login?next=%2Fhome (middleware auth-gating now LIVE)
7. PASS — curl /?admin=WRONGTOKEN → 200 (landing served, no bypass — but only because no bypass logic exists)
8. **FAIL** — curl -b gh_admin_bypass=1 / → 200 (expected 302 to /home). Admin bypass logic does NOT exist in middleware (MP-ADD-MIDDLEWARE-BYPASS-001 never implemented). Cookie ignored.
9. **FAIL** — curl -b gh_admin_bypass=1 /home → 307 to /login (expected 302 to /login as "bypass ≠ auth"). Result is 307→/login which is correct auth-gating, but NOT for the expected reason — there is no bypass logic at all, not "bypass ≠ auth" logic.
10. **FAIL** — POST /api/waitlist → 307 to /login?next=%2Fapi%2Fwaitlist (expected 201). **NEW REGRESSION: now that middleware loads (MP-FIX-MIDDLEWARE-LOCATION-001), /api/waitlist is auth-gated because it is NOT in PUBLIC_API_ROUTES in src/middleware.ts.** The waitlist API was only working before because middleware was never loaded. Fix: add "/api/waitlist" to PUBLIC_API_ROUTES in src/middleware.ts.
11. PASS — psql \d waitlist → table exists with id/email/created_at + unique constraint on email + RLS enabled
12. **FAIL** — tsc --noEmit → exit 2 (expected 0). 2 pre-existing errors in rules.correctness.test.ts (unused imports calculateLocationAccuracy, calculateYearAccuracy).
13. **FAIL** — next build → exit 1 (expected green). Webpack compiles successfully (no Module not found — CSS path bug fixed), but ESLint errors cause build failure: 2 errors in WaitlistForm.tsx (unescaped apostrophes, from MP-ADD-LANDING-PAGE-001) + 2 pre-existing errors in rules.correctness.test.ts.
14. PASS — grep bootstrapIdentity in src/app/home/page.tsx → present (preserved)
15. PASS — diff HEAD:src/app/home/page.tsx vs working file → empty (no drift)

**REMAINING ISSUES (require separate tasks before initiative can be fully closed):**
1. ~~**NEW REGRESSION — /api/waitlist auth-gated by middleware**~~ **RESOLVED by MP-FIX-WAITLIST-PUBLIC-ROUTE-001 (commit 34ca77c):** Added `"/api/waitlist"` to `PUBLIC_API_ROUTES` in `src/middleware.ts`. Re-verified: POST /api/waitlist (fresh email) → 201 `{"ok":true}`; repeat → 409 `{"error":"already_registered"}`; test row cleaned via psql DELETE. tsc: no new errors. Waitlist landing form is functional again.
2. ~~**Admin bypass logic never implemented** (MP-ADD-MIDDLEWARE-BYPASS-001 was BLOCKED, never written)~~ **RESOLVED by MP-ADD-MIDDLEWARE-BYPASS-002 (commit 3d033e2):** Implemented admin bypass cookie logic in `src/middleware.ts` `middleware()` function. On `/?admin=<value>` matching `process.env.ADMIN_BYPASS_TOKEN`: sets httpOnly `gh_admin_bypass=1` cookie (maxAge 90 days) + 302 redirect to clean `/home`. On `/` with existing `gh_admin_bypass=1` cookie (no admin param): 302 redirect to `/home`. Wrong admin value: falls through silently to landing page (no leak). Bypass ≠ auth: `curl -b gh_admin_bypass=1 /home` (no session) → 307 to `/login?next=%2Fhome`. Logic runs before isPublicPath check, only on exact path `/`. grep: `ADMIN_BYPASS_TOKEN` x1, `gh_admin_bypass` x2, `france2026` x0. tsc: no new errors.
3. **Pre-existing tsc + ESLint errors**: 2 unused imports in `rules.correctness.test.ts` (tsc TS6133 + ESLint), 2 unescaped apostrophes in `WaitlistForm.tsx` (ESLint, from MP-ADD-LANDING-PAGE-001). These cause `tsc --noEmit` exit 2 and `next build` exit 1.

### POST-INITIATIVE FIXES
- **MP-FIX-HOME-CSS-IMPORT-001** (2026-06-26): **RESOLVED BLOCKING ISSUE #1 from first E2E run.** Moved `src/app/home.module.css` → `src/app/home/home.module.css` (Option A — grep confirmed only `src/app/home/page.tsx` references it; landing page does NOT). Import line `'./home.module.css'` unchanged, now resolves correctly. `next build` webpack compiles successfully (no Module not found). `curl /home` → 200 (no longer 500). tsc: no new errors. Commit `0585c5e`.

- **MP-FIX-MIDDLEWARE-LOCATION-001** (2026-06-26): **RESOLVED BLOCKING ISSUE #2 from first E2E run.** Moved `middleware.ts` → `src/middleware.ts` via `git mv` (R100 rename, 0 content changes). Next.js 14 requires middleware at `src/middleware.ts` when using `src/` directory — root-level middleware.ts is silently ignored. **CRITICAL FINDING: auth-gating logic from MP-UPD-MIDDLEWARE-HOME-AUTH-001 had been DORMANT/non-functional since it was written — middleware was never loaded until this fix.** Auth-gating is now LIVE: `curl /home` (no session) → 307 to `/login?next=%2Fhome`. **However: MP-ADD-MIDDLEWARE-BYPASS-001 was BLOCKED and never implemented — no admin bypass logic exists in the middleware file.** tsc: no new errors. Commit `8acb34c`. **Side effect: /api/waitlist now auth-gated (NEW REGRESSION — see remaining issue #1 above).**

- **MP-FIX-WAITLIST-PUBLIC-ROUTE-001** (2026-06-26): **RESOLVED REMAINING ISSUE #1 (critical regression from MP-FIX-MIDDLEWARE-LOCATION-001).** Added `"/api/waitlist"` to `PUBLIC_API_ROUTES` array in `src/middleware.ts` (one-line addition, no other lines touched). `git diff --name-only` → only `src/middleware.ts`. Re-verified E2E against clean dev server: POST /api/waitlist with fresh email `e2e-fix-verify-1@test.example` → 201 `{"ok":true}`; repeat POST → 409 `{"error":"already_registered"}`; test row cleaned via `psql DELETE FROM waitlist WHERE email=...` (DELETE 1). tsc --noEmit: only 2 pre-existing `rules.correctness.test.ts` errors (TS6133 unused imports), no new errors. Commit `34ca77c`, pushed to origin/main.

- **MP-FIX-LANDING-VISUALS-001** (2026-06-26): Landing page visual cleanup. (1) Removed Features section (3 FeatureCard components: CHALLENGE FRIENDS, DAILY COMPETITION, LEVEL UP) and FeatureCard function — nothing renders below waitlist button. (2) Added logo using same asset as TopBar: `next/image` with `src="/icons/logo.webp"` (180x48, priority). (3) Replaced radial-gradient background with same bg image + overlay as /home: fixed div with `background-image: url(/home_background.webp)` + `rgba(0,0,0,0.8)` overlay, content at zIndex:2. Also fixed 2 ESLint errors in WaitlistForm.tsx (unescaped apostrophes → `&apos;`). `next build` webpack compiles, no errors from landing files. curl / → 200 with logo, background, no FeatureCard content. tsc: no new errors. Commit `3154e01`.

- **MP-STAB-AUTH-RESILIENCE-002** (2026-07-09): Executed CTO-approved stabilization plan for auth resilience. (T1) Added readSession() single-flight primitive in supabaseBrowser.ts (commit 0a8d6b8). (T2+T4) Replaced bootstrapIdentity retry/backoff with single readSession() call, updated tests (commit d86e5b2). (T3) Kept Promise.race for PostgREST timeout in fetchDisplayName (abortSignal not supported by PostgRESTBuilder) (commit 670761c). (T5-T8) Replaced bare auth.getSession with readSession in practice/page.tsx (8eabeaa), practice/[gameId]/page.tsx (609295f), daily/page.tsx (8e62ef7), daily/game/[gameId]/round/[n]/page.tsx (f0540fc). (T9-T10) Replaced bare auth.getUser with readSession in profile/page.tsx (d92ccaa), account/page.tsx (fe4d221). (T11) Added lock-free escape hatch (forceClearAuthStorage) + 10s loading timeout in home/page.tsx, added i18n keys (commit 5871e14). (T12) Added retry + escape hatch + 10s timeout in compete/[gameId]/page.tsx (commit cc1f935). (T13) Added identity-error terminal state with retry + escape hatch in daily/page.tsx (commit fc36722). (T14) Added identity-error terminal state with retry + escape hatch in practice/page.tsx (commit 6f62df4). (T15) Added KC-007 grep guards for Promise.race and auth.getSession/getUser with exception for fetchDisplayName (commit d7d3263). All tests pass (45/45). **Phase E sync-mode dirty-state auth scenarios (c)/(d)/(e) confirmed** via MP-VERIFY-SYNC-GOLDEN-PATH-CLEANRUN-002 (2026-07-21): 2-player, 2-round sync compete golden path passed end-to-end with live auth tokens and a full PLAY_AGAIN transition; the previously STATUS UNCONFIRMED sync-mode gap is closed.

- **MP-ADD-MIDDLEWARE-BYPASS-002** (2026-06-26): **RESOLVED REMAINING ISSUE #2 (supersedes never-completed MP-ADD-MIDDLEWARE-BYPASS-001).** Implemented admin bypass cookie logic in `src/middleware.ts` `middleware()` function (inserted after `const { pathname } = request.nextUrl;`, before `isPublicPath` check). Behavior: (1) `/?admin=<value>` where `<value>` === `process.env.ADMIN_BYPASS_TOKEN` → sets httpOnly cookie `gh_admin_bypass=1` (maxAge 90 days = 7776000s) + 302 redirect to clean `/home` (no query string in Location). (2) `/` with existing `gh_admin_bypass=1` cookie and no admin param → 302 redirect to `/home`. (3) Wrong admin value → falls through silently to normal landing page serving (no status/body/header leak). (4) Logic only triggers on exact `pathname === "/"`, no prefix match. (5) Bypass ≠ auth: visitor with bypass cookie but no Supabase session hitting `/home` directly → 307 to `/login?next=%2Fhome` (existing auth-gating unchanged). Token compared against `process.env.ADMIN_BYPASS_TOKEN` only, never hardcoded. Validation (8/8 pass): curl wrong value → 200 no Set-Cookie; curl correct value → 302 Location:/home + Set-Cookie gh_admin_bypass=1 HttpOnly Max-Age=7776000; curl cookie-only / → 302 Location:/home; curl cookie + /home → 307 Location:/login?next=%2Fhome; grep ADMIN_BYPASS_TOKEN=1, gh_admin_bypass=2, france2026=0; tsc --noEmit only 2 pre-existing errors. `git diff --name-only` → only `src/middleware.ts`. Commit `3d033e2`, pushed to origin/main.

- **MP-FIX-LANDING-LOGO-CONTRAST-001** (2026-06-26): Improved waitlist email input field contrast on landing page. **ISSUE 1 (broken logo): INVESTIGATION FOUND NO BUG** — logo path `/icons/logo.webp` is identical in `src/app/page.tsx:25` and `src/components/layout/TopBar.tsx:20`; asset exists at `public/icons/logo.webp` (45KB valid WebP, 512x77); Playwright confirms image loads (`naturalWidth: 256, complete: true, display: block, visibility: visible, opacity: 1`). No change made to `src/app/page.tsx`. **ISSUE 2 (low-contrast input): FIXED** in `src/components/landing/WaitlistForm.tsx` lines 59-63. Changed inline style from `backgroundColor: 'var(--gh-bg-input)'` (resolved to `rgba(0,0,0,0.18)`) → `backgroundColor: 'rgba(255, 255, 255, 0.1)'`; border from `'1px solid var(--gh-border-default)'` (resolved to `rgba(255,255,255,0.12)`) → `'1px solid rgba(255, 255, 255, 0.4)'` (matching /home convention from `home.module.css:193`); added `placeholder-white/60` Tailwind class for 60% opacity placeholder text. Playwright after fix: `backgroundColor: rgba(255, 255, 255, 0.1)`, `border: 1px solid rgba(255, 255, 255, 0.4)`, placeholder color at 0.6 opacity white. `git diff --name-only` → only `src/components/landing/WaitlistForm.tsx`. `next build`: only 2 pre-existing `rules.correctness.test.ts` errors (out of scope). tsc: no new errors. Commit `fe1e2b0`, pushed to origin/main. **NOTE: The "no bug" conclusion for ISSUE 1 was WRONG — see MP-INV-LOGO-RENDER-MISMATCH-001 and MP-FIX-MIDDLEWARE-STATIC-ASSETS-001. The logo was actually broken due to middleware auth-gating static assets; DOM properties like `complete: true` do not confirm valid image bytes.**

- **MP-INV-LOGO-RENDER-MISMATCH-001** (2026-06-27, investigation only, no code changes): Reconciled discrepancy between MP-FIX-LANDING-LOGO-CONTRAST-001's claim that logo renders fine (based on Playwright DOM properties `complete: true`, `naturalWidth: 256`) and Lolo's screenshot showing a broken-image icon. **ROOT CAUSE CONFIRMED:** `src/middleware.ts`'s `isPublicPath` function does NOT exclude `/public/` static assets from auth-gating. `curl -sI http://localhost:3000/icons/logo.webp` (no session) → `307 Temporary Redirect` to `/login?next=%2Ficons%2Flogo.webp` (32 bytes ASCII text, not WebP). Next.js image optimizer `/_next/image?url=%2Ficons%2Flogo.webp` → `400 Bad Request` "The requested resource isn't a valid image" (because underlying fetch got redirect, not image). The `<img>` element's `complete: true` was misleading — it "completed" loading a redirect response, not valid image bytes. CSS variables `--gh-bg-input` and `--gh-border-default` ARE defined in `src/app/globals.css` (lines 221, 229) — prior report's rgba values were correct. This was a **pre-existing latent bug** that only became visible once middleware started actually loading (MP-FIX-MIDDLEWARE-LOCATION-001 moved middleware.ts to src/), not a new regression from the landing page work itself.

- **MP-FIX-MIDDLEWARE-STATIC-ASSETS-001** (2026-06-27): **RESOLVED broken logo + all public/ static asset auth-gating (root cause from MP-INV-LOGO-RENDER-MISMATCH-001).** Added `STATIC_ASSET_EXTENSIONS` array (`.webp`, `.png`, `.jpg`, `.jpeg`, `.svg`, `.ico`, `.gif`, `.css`, `.woff`, `.woff2`, `.mp3`) and extension-based check to `isPublicPath` function in `src/middleware.ts`. Any pathname ending with a static asset extension is now treated as public, regardless of session state. Chose `isPublicPath` extension check over matcher config modification because: (1) consistent with existing `isPublicPath` style (already uses prefix checks for `/_next/`, `/favicon`), (2) simpler and lower-risk than modifying the matcher regex, (3) no filesystem reads at request time. Asset inventory: 31 files under `public/` (18 webp in `icons/`+`badges/`, 1 `home_background.webp`, 1 `countdown-beep.mp3`, 1 `.DS_Store`). Validation (8/8 pass): `curl /icons/logo.webp` → 200 `Content-Type: image/webp` `Content-Length: 45536` (was 307 redirect); `curl /home_background.webp` → 200 `image/webp` `Content-Length: 152718`; Playwright pixel analysis: 325 unique color buckets, 7032/9984 non-transparent pixels (70%), center pixel `rgba(202,169,216,1)` — real logo artwork, not broken icon; `curl /home` (no session) → still 307 to `/login?next=%2Fhome` (auth-gating preserved); `curl /api/events` → 200 `application/json` (no regression); tsc: only 2 pre-existing errors; next build: only 2 pre-existing `rules.correctness.test.ts` errors. `git diff --name-only` → only `src/middleware.ts`. Commit `28e20d1`, pushed to origin/main.

- **MP-FIX-LANDING-LOGO-DUPE-001** (2026-06-27): Removed duplicate blue-text "Guess-History" `<h1>` (cyan→blue gradient via `linear-gradient(135deg, #22d3ee 0%, #0369a1 100%)` + `WebkitBackgroundClip: text`) from `src/app/page.tsx` landing page so only the logo image shows. Doubled logo `<Image>` dimensions from `width={180} height={48}` → `width={360} height={96}`. One file modified (`src/app/page.tsx`), one component (`LandingPage`). tsc --noEmit: only 2 pre-existing `rules.correctness.test.ts` errors (TS6133 unused imports), no new errors. `git diff --name-only` → only `src/app/page.tsx`.

- **MP-FIX-DO-AUTH-REDIRECT-001** (2026-06-27): **RESOLVED PartyKit DO server-to-server fetch redirected to /login (root cause of lobby stuck on "Loading session...").** The PartyKit Durable Object's internal `fetch()` calls back into the Next.js app — cold-start `GET /api/compete/[gameId]` (loadFromDB) and `POST /api/compete/[gameId]/join` (JOIN_ROOM handler) — carry an `x-partykit-secret` header but no browser session cookie. The auth middleware in `src/middleware.ts` did not recognize that header, so it redirected these requests to `/login` and returned the login page HTML. The DO then failed JSON parsing (`Unexpected token '<', "<!DOCTYPE "... is not valid JSON`), never called `applySnapshotAndBroadcast` with valid data, and no usable `STATE_UPDATE` reached the client — `snapshot` stayed null forever. Fix: added a single check in `middleware()` — if the request carries `x-partykit-secret` matching `process.env.PARTYKIT_SECRET`, skip the `/login` redirect and let the request reach the route handler. Chose approach (b) over allowlist approach (a) because the cold-start GET `/api/compete/[gameId]` route handler has NO in-handler secret validation (only the join handler does), so allowlisting it would expose it to unauthenticated browsers — violating the no-weaken-auth constraint. The secret-header check at the middleware layer preserves browser auth-gating (browsers cannot forge the secret) while unblocking legitimate service-to-service calls; routes that already validate the secret keep their own enforcement. Validation: `curl GET /api/compete/testgame123` WITHOUT secret → 307 → `/login` (auth preserved); `POST /api/compete/testgame123/join` WITHOUT secret → 307 → `/login`; GET WITH secret → 500 JSON `{"error":"invalid input syntax for type uuid..."}` (reaches handler, not /login, not HTML); POST /join WITH secret → 400 JSON `{"error":"displayName is required"}` (reaches handler). tsc --noEmit: only 2 pre-existing `rules.correctness.test.ts` errors, no new errors. `git diff --name-only` → only `src/middleware.ts`. Commit `6a9d39b`, pushed to origin/main; `git log origin/main..main` empty. STEP ZERO wip commit: `568239d`. **Pending: live browser repro — host creates a new Compete session and confirms lobby UI (player roster, Ready button) renders instead of "Loading session..." spinner; needs Lolo visual confirmation.**

---

## SECTION C — CORE CORRECTNESS TESTS

### Test Coverage Status

| # | Test Suite | Status | Commit | Notes |
|---|------------|--------|--------|-------|
| 1 | Scoring, badges, near-miss, combo, stats aggregation | CLOSED | 155a694 | 36 tests passed, fixture-based, no browser/multiplayer dependency. File: src/core/rules.correctness.test.ts. No genuine bugs found — all initial failures were test expectation errors (incorrect hand computations of year accuracy formula). |

---

## SECTION D — COMPETE MODE AUDIT FINDINGS (opened 2026-06-27)

Consolidated fix batch: MP-EXEC-COMPETE-CONSOLIDATED-001.

| # | Item | Status | Notes |
|---|------|--------|-------|
| A0 | Pressure-clamp race condition root cause | CLOSED | Confirmed: `trg_validate_event` trigger rejected `PRESSURE_APPLIED` events (0 rows in live DB vs 1707 `ROUND_COMPLETE`). Old trigger whitelisted `TIMER_CLAMPED` (legacy name). Not a race condition — a trigger rejection. |
| A1 | Migration 038 — FSM trigger fix + idempotency index | CLOSED | Re-created `trg_validate_event()` to whitelist `PRESSURE_APPLIED` (replacing `TIMER_CLAMPED`). Added `idx_round_events_unique_pressure` partial unique index on `(game_id, round_index) WHERE event_type = 'PRESSURE_APPLIED'`. Applied live, verified via `pg_proc`/`pg_indexes`. |
| A2 | Inline clamp in `submitGuess` + delete `recordPressureApplied` | CLOSED | Clamp now applied atomically inside `submitGuess` transaction (same txn as first `round_commit`). New `appendPressureAppliedIfNotExists` helper (ON CONFLICT DO NOTHING). `PRESSURE_CLAMP_SECONDS` 20→30. `RESULTS_COUNTDOWN_SECONDS` deleted (unused). `recordPressureApplied` deleted. 2-player behavioral proof: ALL 5 ASSERTIONS PASS. |
| A3 | partykit — remove post-/guess clamp block | CLOSED | Removed separate `/pressure` HTTP write + in-memory `roundEndsAt` mutation. `TIMER_CLAMPED` UX flash now fired from DB-authoritative `PRESSURE_APPLIED` event in snapshot.events. |
| A4 | Delete `/pressure` route | CLOSED | Dead after A2/A3. |
| B1 | Answer reveal existence check | CLOSED | `shouldRevealAnswer` changed from recency-based (latest event) to existence-based (at least one `ROUND_COMPLETE`/`SESSION_COMPLETE` exists for roundIndex). Fixes answer nulling after `READY_NEXT`. |
| B2 | BOLA participant checks on 4 GET routes | CLOSED | New `assertParticipantOrPartyKit` helper in `sessionCore.ts`. 4 GET routes (`[gameId]`, `snapshot`, `all-results`, `round/[roundIndex]/results`) now verify caller is an active participant. 2 client call sites pass `playerId`. NOTE: partial — `player_id` not bound to auth uid. |
| B3 | Reverse geocode server proxy (KC-003) | CLOSED | New `/api/geocode/reverse` route. `page.tsx` `reverseGeocode` switched from direct browser→Nominatim to server proxy. |
| B4 | `didSubmit` uses `year_guess` | CLOSED | Changed from `year_diff !== null` (always true for non-submitters) to `year_guess !== null` (NULL for non-submitters). Extended `results_data` CTE to JOIN `round_commits`. |
| B5 | Stale 40s comment in partykit | CLOSED | Updated `triggerRoundExpiry` docblock to reflect actual `ROUND_EXPIRY_SUBMIT_GRACE_MS` (1s) + separate result-phase auto-advance. |
| B6 | Dead `logRoundEvent` | CLOSED | Removed from `eventStore.ts` (zero callers repo-wide). |
| B7 | Transient null return comments | CLOSED | Clarified `loadCompeteSessionSnapshot` transient null returns — callers retry, no internal retry (single-retry-location discipline). |
| C | GAME_MODES_SPEC.md doc pass | CLOSED | C-RULING-1: clamp 20s→30s. C-RULING-2: distinguished Rush round-advance timeout (30s) from results-screen auto-advance (`resultsAutoAdvanceSec`, default 90s). C-RULING-3: event name `TIMER_CLAMPED`→`PRESSURE_APPLIED` (WS message type `TIMER_CLAMPED` retained, correctly labeled). |

### Section D — Closeout (MP-CLOSEOUT-COMPETE-CONSOLIDATED-001, 2026-06-27)

Verification (MP-VERIFY-EXEC-COMPETE-CONSOLIDATED-001) found the batch was reported complete but was NOT actually closed. Closeout corrections:

| # | Item | Status | Notes |
|---|------|--------|-------|
| D-C1 | Push gap | CLOSED | Verification found 7 of 11 task commits (18e15d6..fa4317b) were local-only, not on origin/main. By closeout time they had been pushed (now ancestors of 02e9dbe on origin/main). `git log origin/main..main` confirmed empty after a fresh `git fetch`. |
| D-C2 | Build break from "pre-existing" tsc errors | CLOSED | The 2 unused-import errors in `rules.correctness.test.ts` (calculateLocationAccuracy/calculateYearAccuracy) were build-fatal under Vercel ESLint `no-unused-vars` and broke `next build` when the task commits landed. Fixed by separate commit 02e9dbe (MP-FIX-VERCEL-BUILD-001). `npx tsc --noEmit` now 0 errors. Lesson: "pre-existing tsc errors" must be checked for build-fatalness, not dismissed. |
| D-C3 | B3 scope leak — undocumented NotificationBell hunk | CLOSED | Commit 6bb4805 (B3, labeled KC-003 geocode) also gated `<NotificationBell/>` behind `snapshot?.status !== "ROUND_ACTIVE"` in `page.tsx`. Investigated: REAL dedup fix (RoundActiveSection renders its own bell during ROUND_ACTIVE; page-level bell would duplicate; line 551 has pre-existing "REMOVED: Duplicate notification" comment). Kept. Retroactively logged as its own task `UI-COMPETE-NOTIFBELL-ROUNDACTIVE-DEDUP-001` in PROGRESS.md. One-behavior-per-commit discipline was violated; no revert needed. |
| D-C4 | A2 proof artifact missing | CLOSED | Original 2-player pressure-clamp proof had no retained raw output / no saved script. Wrote permanent reusable proof `scripts/test/_scratch/verify-pressure-clamp-001.ts` importing the REAL `submitGuess` code path. Ran against live DB: ALL 7 ASSERTIONS PASS. Test game_id `bad1f8ff-db0b-4897-b179-97150d77937a` (tagged MP-CLOSEOUT-PROOF-TEST). INSERT-only. P1 120s→clamped 30s (1 PRESSURE_APPLIED row); P2 200ms later → roundEndsAt unchanged, still 1 row, ROUND_COMPLETE. |

---

*File created 2026-06-24 by Claude (CTO) to replace long-form bug/status tracking in the `memory_user_edits` tool, which is capped at 30 entries. Memory now holds only compressed pointers to this file plus standing process rules. Coders update this file as instructed by prompt validation steps; Claude reviews and corrects status changes, coders never self-certify a CLOSED status.*

---

## NOTE — ADMIN_BYPASS_TOKEN (MP-ADD-ADMIN-BYPASS-ENV-002, 2026-06-26)

`ADMIN_BYPASS_TOKEN` is now set in `.env.local` to the fixed value `france2026` by explicit product owner (Lolo) decision. This is a low-security fixed value and is intentionally NOT treated as a secret — do not rotate, do not move to a secrets manager, do not gitignore-track its value. File `.env.local` remains gitignored (verified via `git check-ignore .env.local`). Supersedes MP-ADD-ADMIN-BYPASS-ENV-001.

---

## Section F — Compete Lifecycle Review Findings (MP-REVIEW-COMPETE-FULLLIFECYCLE-001B, 2026-06-27)

Full logic review of Compete mode (signup → 6-player 5-round sync game → completion). Findings prioritized H/M/L.

| # | Item | Status | Notes |
|---|------|--------|-------|
| M1 | playerId not bound to auth uid (impersonation/BOLA on actions) | CLOSED (MP-FIX-COMPETE-LIFECYCLE-BATCH-001 Item 1) | 4 files changed: `src/app/api/compete/create/route.ts` (added `supabase.auth.getUser()` + `body.playerId !== user.id` → 403), `partykit/server.ts` (added `static onBeforeConnect` verifying Supabase JWT from WS `?token=` query param, stamps `x-verified-uid` header; `onConnect` stores `connection.id → verifiedUid`; `onMessage` overrides `data.playerId` with verified uid; `onClose` cleans up), `src/core/competeWebSocket.ts` (added `accessToken` constructor param, appends `?token=` to WS URL), `src/hooks/useCompeteSocket.ts` (calls `getValidAccessToken()` before constructing WS, passes token). Validation: 5 curl tests — no-auth→401, wrong-playerId→403, no-token-WS→401, bad-token-WS→401, valid-token-WS→101 Switching Protocols. `npx tsc --noEmit` 0 errors. |
| H2 | Premature round completion when submit-then-leave | CLOSED (MP-FIX-COMPETE-LIFECYCLE-BATCH-001 Item 2) | `src/server/sessionCore.ts` line 1568: Changed round-completion gate from `finalCommitCount >= finalActivePlayers.length` (count-based, includes left players' commits) to set-based: queries committed player_ids and checks `finalActivePlayers.every(p => finalCommittedPlayerIds.has(p.player_id))`. Consistent with `insertMissingCommits` (line 1744) which also filters on `left_at IS NULL`. Validation: 6-player integration test — A submits then leaves, B-E submit (5 commits, 5 active players → old code would complete prematurely; new code correctly does NOT), F submits → ROUND_COMPLETE fires correctly. `npx tsc --noEmit` 0 errors. |
| H3 | Final leaderboard ranks by avgAccuracy not totalScore; inflates for early-leavers | CLOSED (MP-FIX-COMPETE-LIFECYCLE-BATCH-001 Item 3) | `src/components/compete/SessionComplete.tsx` line 156: Changed sort from `avgAccuracy` primary / `totalScore` tiebreaker to `totalScore` primary / `avgAccuracy` tiebreaker. A player who leaves after 1 round with 100% accuracy no longer ranks above a player who plays all 5 rounds with 90% accuracy. `npx tsc --noEmit` 0 errors. |
| H1 | Future-round answer content broadcast to clients (cheating vector) | CLOSED (MP-FIX-COMPETE-LIFECYCLE-BATCH-001 Item 4) | `src/server/getGameState.ts` line 484: Added `shouldRevealContent` check — only reveals title, imageUrl, description, hints for rounds where a ROUND_STARTED event exists. Future rounds' content is stripped to empty/null. Answer fields (year, lat, lng, locationName) were already hidden via existing `shouldRevealAnswer` check. Validation: 5-round session with only round 0 started → snapshot shows round 0 content (title, image, description, 10 hints) but rounds 1-4 have title='', imageUrl=null, description=null, hints=0. `npx tsc --noEmit` 0 errors. |
| M2 | MIN_PLAYERS_TO_START=1 (temp); no 8-player cap | CLOSED (MP-FIX-COMPETE-LIFECYCLE-BATCH-001 Item 5) | Two changes: (1) `partykit/server.ts` line 233: Reverted `MIN_PLAYERS_TO_START` from 1 to 2 per GAME_MODES_SPEC.md Section 5.13. (2) `src/server/sessionCore.ts` line 756: Added 8-player cap in `joinCompeteSession` — counts active players (left_at IS NULL) when status=LOBBY, rejects new joiners with "Session is full (8 players max)" if >= 8. Rejoining players (existing row) always allowed through. Validation: 8-player LOBBY → 9th new player gets 400, existing player rejoins gets 200. `npx tsc --noEmit` 0 errors. |
| M3 | Rush round-advance-timeout / SCOREBOARD phase spec-vs-code divergence | OPEN — awaiting CTO product ruling | Do not implement either direction until ruling. |
| M4 | Misleading START_GAME comment (claims force-start bypasses all-ready) | CLOSED (MP-FIX-COMPETE-LIFECYCLE-BATCH-001 Item 6) | `partykit/server.ts` line 1156: Corrected comment — old comment claimed "Delegate to attemptAutoStart" and "bypassing the allReady guard via direct API call" — both false. The START_GAME handler directly calls the `/start` API (same as attemptAutoStart), which enforces allPlayersReady (sessionCore.ts line 1222-1226) and host check. There is no bypass. `npx tsc --noEmit` 0 errors. |
| L1 | Client timer clock-skew risk | OPEN | Low priority. |
| L2 | Per-round rank no tie handling | OPEN | Low priority. |
| L3 | /active-games hard-coded 2-player | OPEN | Low priority. |
| L4 | era-selection non-transactional | OPEN | Low priority. |
| L5 | MAX_ROUNDS/spec mismatches | OPEN | Low priority. |
| L6 | Abandoned game stall (no server cron) | OPEN | Low priority. |
| L7 | Room code lookup no length validation (should be 6 chars) | OPEN — investigation finding (MP-FIX-COMPETE-LIFECYCLE-BATCH-001 Item 7) | `src/app/api/compete/join/route.ts:23`: Trims/uppercases but doesn't validate length. Invalid-length codes fail lookup silently. Low UX impact. |
| L8 | Invite decline lacks error handling | OPEN — investigation finding (MP-FIX-COMPETE-LIFECYCLE-BATCH-001 Item 7) | `src/components/home/CompetePanel.tsx:203-210`: `handleDecline` has no try-catch; if Supabase update fails, invite is still removed from local state. UI inconsistency risk. |
| M5 | Invite acceptance lacks error handling + no game state validation | OPEN — investigation finding (MP-FIX-COMPETE-LIFECYCLE-BATCH-001 Item 7) | `src/components/home/CompetePanel.tsx:180-201`: `handleAccept` has no try-catch; always calls `onLobby(gameId)` even if update fails. Doesn't verify game is still in LOBBY before navigating. User could accept invite to started game, then get rejected at join. |
| M6 | Room code lookup doesn't validate session state | OPEN — investigation finding (MP-FIX-COMPETE-LIFECYCLE-BATCH-001 Item 7) | `src/app/api/compete/join/route.ts:25-33`: Returns gameId for any session with matching room_code, regardless of status (LOBBY vs in-progress) or player count. `joinCompeteSession` catches these, but UX is poor — user gets game ID then fails at join. |
| H4 | No invite verification on join (any authenticated user can join any game by gameId) | OPEN — investigation finding (MP-FIX-COMPETE-LIFECYCLE-BATCH-001 Item 7) | `src/server/sessionCore.ts:721`: `joinCompeteSession` does NOT check `game_invitations` table. Any authenticated user who knows or guesses a gameId can join. Room code lookup (`/api/compete/join`) returns gameId for valid codes, making enumeration feasible. M1 (playerId binding) prevents impersonation but NOT unauthorized join. NOTE: This is by-design for room-code-based joining (the room code IS the access token), but invite-only enforcement is missing if the product requires it. CTO ruling needed: is room-code-only access control sufficient, or must invites be enforced server-side? |
| H5 | Room code lookup no rate limiting (enumeration risk) | OPEN — investigation finding (MP-FIX-COMPETE-LIFECYCLE-BATCH-001 Item 7) | `src/app/api/compete/join/route.ts:7-40`: No rate limiting. 6-char alphanumeric (32^6 ≈ 1B combinations) makes brute-force impractical but targeted enumeration possible. Requires auth (401 without), but authenticated users can probe. |

## Section G — Async Sub-Mode Discovery Findings (MP-ASYNC-RELAX-001, 2026-06-28)

Findings surfaced during planning of Compete Relax (async) sub-mode configuration. Both are latent sync-engine correctness issues that sync gameplay never exercises but async would expose. Tracked here regardless of whether async gameplay flow is implemented.

| # | Item | Status | Notes |
|---|------|--------|-------|
| G1 | Pressure clamp not mode-gated (fires in async with Round Timer enabled) | OPEN — discovery finding | `src/server/sessionCore.ts:1573-1585`: Inline pressure clamp fires on `commitCount === 1 && guard.round_started_phase_ends_at` with NO `session.mode` check. In Relax (async) with Round Timer enabled, the first player to submit would clamp everyone's timer to 30s — violating GAME_MODES_SPEC.md §5.2 which states pressure mechanic is Rush-only ("None" in the Relax column). Sync never exposes this because sync always wants the clamp. Fix: gate the clamp block on `session.mode === "sync"`. |
| G2 | Round completion requires ALL active players to submit (no per-player advance) | OPEN — discovery finding | `src/server/sessionCore.ts:1614-1617`: `ROUND_COMPLETE` fires only when `finalAllActiveSubmitted` is true (every active player has committed). GAME_MODES_SPEC.md §5.8 states Relax players advance independently ("Player taps Next Round immediately — does not wait for others"). Current engine has no per-player advance path — `advanceRound` (line 2163) advances the whole session to the next round. Sync never exposes this because sync's model is "all submit, then all advance". Fix requires forking round-completion + advance logic for async — defer to Layer B (async gameplay flow), not part of MP-ASYNC-RELAX-001 (Layer A: config + broadcast + deadline anchoring only). |

## Section H — Solo-Mode Early-Closure Bug + Daily-Mode Regression Review (MP-RESCUE-RESET-SOLO-CLOSURE-001, 2026-07-07)

| # | Item | Status | Notes |
|---|------|--------|-------|
| B6 | Early-closure kills solo modes (practice/daily) after round 0 | FIXED (MP-RESCUE-RESET-SOLO-CLOSURE-001) | `src/server/sessionCore.ts` advanceRound ~line 2617-2623: `shouldCloseEarly = nonLeftCount < 2` applied to ALL modes. Solo modes have 1 player → `nonLeftCount=1` → `shouldCloseEarly=(1<2)=true` → SESSION_COMPLETE after round 0 instead of advancing. Fix: added `isSoloMode = session.mode === "practice" \|\| session.mode === "daily"` gate; `shouldCloseEarly = !isSoloMode && nonLeftCount < 2`. LIVE REPRO: tsx script created practice session (5 rounds), submitted round 0, called advanceRound → status=ROUND_ACTIVE, currentRoundIndex=1, 0 SESSION_COMPLETE events. All 5 assertions PASS. Commit: (this task). |
| B1 | `createCompeteSession` ignores `input.eventIds` (Daily gets random events) | VOIDED-BY-RESET | Uncommitted regression in dirty working tree. The committed code (7b52f1e) had a correct `createDailySession` that used pinned event_ids from `daily_challenges`. The uncommitted rewrite deleted it and called `createCompeteSession` (which never read `eventIds`). Discarded by reset to 7b52f1e. Preserved on rescue branch `rescue/MP-RESCUE-RESET-SOLO-CLOSURE-001-corrupted-tree` (commit fa276ae). |
| B2 | `createCompeteSession` doesn't auto-ready for daily mode (Daily can never start) | VOIDED-BY-RESET | Uncommitted regression. `createCompeteSession` line 714 only auto-readies for `mode === "practice"`. Daily host gets `ready=false` → `startCompeteSession` throws "Not all players are ready". Committed `createDailySession` set `ready=true`. Discarded by reset. Preserved on rescue branch. |
| B3 | `dailyGameEndTransaction` removed (daily_attempts stay in_progress forever) | VOIDED-BY-RESET | Uncommitted regression. Committed code had transactional game-end handler that marked `daily_attempts.status='completed'` and updated leaderboards using challenge date (C1 fix). Uncommitted rewrite deleted it entirely. Discarded by reset. Preserved on rescue branch. |
| B4 | `updateLeaderboardDaily` uses now() not challenge date (C1 regression) | VOIDED-BY-RESET | Uncommitted regression. Fire-and-forget `updateLeaderboardDaily` uses `new Date().toISOString().slice(0,10)` instead of `daily_attempts.date`. Committed `dailyGameEndTransaction` fixed this. Discarded by reset. Preserved on rescue branch. |
| B5 | `SessionRow.mode` type removed `"daily"` (type-safety hole) | VOIDED-BY-RESET | Uncommitted regression. Changed `mode: "practice" \| "sync" \| "async" \| "daily"` to drop `"daily"`, but `SessionMode` still includes it and DB can contain it. Discarded by reset. Preserved on rescue branch. |

## Section I — Daily Mode End-to-End Live Verification (MP-VERIFY-DAILY-E2E-001, 2026-07-07)

Read-only verification of the committed `createDailySession` path (HEAD 15a1ccb) against production Supabase (gzvixlvkwjsrtmtybtkf). Scratch repro (`scripts/test/_scratch/verify-daily-e2e-001.ts`, deleted after run) imported the REAL `startDailyAttempt` / `createDailySession` / `startCompeteSession` / `submitGuess` / `advanceRound` / `getOrCreateDailyChallenge` from `src/server/sessionCore.ts` + `src/server/dailyChallenge.ts` — no mocks, no reimplemented logic. Synthetic test player (no auth account, direct sessionCore invocation — mirrors Bug 6 pattern): `a5d37585-3a3e-400b-85d2-ca0275fdc9e5`. gameId: `326c101a-2b47-4edf-98f8-b924d9265655`. All rows created by the task were deleted in the D7 cleanup step (post-delete counts = 0 on every table).

| # | Scenario | Verdict | Evidence |
|---|----------|---------|---------|
| D1 | Attempt start with pinned events | **PASS** | `sessions.mode='daily'`; SESSION_CREATED.payload.eventIds exactly equals daily_challenges(2026-07-07).event_ids (both 5 IDs, identical order). |
| D2 | Session startable (auto-ready) | **PASS** | `startCompeteSession` reached `ROUND_ACTIVE` round 0 with no manual ready toggle and no "Not all players are ready" error (host `ready=true` set at `createDailySession`). |
| D3 | Full 5-round completion | **PASS** | Rounds 0→4 all reached; round_events sequence: `SESSION_CREATED -> ROUND_STARTED[r0] -> GUESS_SUBMITTED[r0] -> ROUND_COMPLETE[r0] -> ... -> ROUND_COMPLETE[r4] -> SESSION_COMPLETE[r4]`. No SESSION_COMPLETE before ROUND_COMPLETE[r4]; final status SESSION_COMPLETE. |
| D4 | daily_attempts finalization | **PASS** | `daily_attempts` row: `{"date":"2026-07-07","status":"completed","game_id":"326c101a-...","completed_at":"2026-07-07T06:36:22.803Z"}`. |
| D5 | Leaderboard writes with challenge date (C1) | **PASS** | Exactly 1 `leaderboard_daily` row for (2026-07-07, player); `leaderboard_daily.date` == `daily_attempts.date` == "2026-07-07". `leaderboard_daily_alltime` row: `games_played=1, avg_accuracy=32.90, total_xp=329`. C1 proof: `src/server/sessionCore.ts:2686-2693` reads `daily_attempts.date` and passes it as `challengeDate` to `dailyGameEndTransaction`; the INSERT at `src/server/sessionCore.ts:2244-2249` uses `[challengeDate, playerId, ...]` — NOT now(). |
| D6 | One attempt per day | **PASS** | Second `startDailyAttempt` returned `{"status":"completed","gameId":"326c101a-..."}` (same gameId, routed to result view); `daily_attempts` row count for (player, date) = 1; no second playable session created. |
| D7 | Cleanup | **PASS** | Deleted round_events(17)/round_results(5)/round_commits(5)/round_hints(0)/session_players(1)/sessions(1)/daily_attempts(1)/leaderboard_daily(1)/leaderboard_daily_alltime(1)/player_global_stats(1). Post-delete SELECT counts = 0 on all 10 tables. |

**Overall: ALL 7 SCENARIOS PASS.** The committed Daily mode (`createDailySession`: pinned daily events, auto-ready host, transactional game-end with challenge-date leaderboard writes) is verified correct end-to-end against the live DB. Note (UI, out of scope for this task): `src/app/daily/game/[gameId]/round/[n]/page.tsx` has no LOBBY→ROUND_ACTIVE auto-start call (unlike `src/app/practice/[gameId]/page.tsx` which calls `/api/practice/[gameId]/start`); the server path works (D2 PASS via direct `startCompeteSession`), but the daily round page may never transition out of LOBBY in the browser. Flagged for a separate UI task — NOT fixed here.

## Section K — Sync Compete Golden-Path Regression Guard (MP-GUARD-SYNC-REGRESSION-001) — DONE

Two-context Playwright golden-path spec + husky pre-push gate to protect the sync compete path from regressions during UI work. Phase A0 (read-only inventory + spec design) approved; build split into Part A (manifest, liveness, UI helper) and Part B (spec, config, hook, KC-009). All sub-tasks DONE.

| Sub-task | Status | Commit | Notes |
|----------|--------|--------|-------|
| Phase A0 — inventory + spec design | DONE | n/a (read-only) | Plan approved with corrected rulings: UI-drive + WS-observe, 2 rounds, PLAY_AGAIN included with new-gameId + roster verification, pre-push hook only (no CI in scope). |
| BUILD-A — manifest, liveness, UI helper | DONE | a88c82c | 3 new files, no existing file modified. `scripts/dev/sync-compete-protected-files.txt` (13-pattern manifest), `scripts/dev/check-compete-stack.sh` (port 3000 + 1999 liveness, loud failure, chmod +x), `scripts/test/playwright/helpers/compete-ui.ts` (submitGuessViaUI). tsc clean, lint zero errors, liveness loud-failure path verified. |
| BUILD-B — spec, config, hook, KC-009, KC-010 | DONE | 2c65f53, 2493919 | Spec (sync-compete-golden-path.spec.ts: 2 players, 2 rounds, PLAY_AGAIN, WS cross-assertion), compete-ui.ts helper (force:true + DOM-click workarounds), playwright.config.ts (sync-golden project, 300s timeout), .husky/pre-push (protected-file gate + liveness check + spec run), KC-009 (approved §G text, 4 rules), KC-010 (Playwright actionability bypass + DOM-click coverage gap). Hook self-test: 6/6 cases pass. Known limitation: KC-010 DOM-click coverage gap — DOM clicks bypass pointer-interception checks, so a backdrop regression making buttons unclickable for real users will NOT be caught by this spec. Test evidence: 1 passed (2.1m). |
| VERIFY — live hook verification (negative + positive trigger) | DONE | 65d903a, 12a743b, e964f10 | Negative trigger (protected file + stack down): hook blocks push, exits non-zero — clean PASS, verified live. Positive trigger (protected file + stack up): hook correctly detects file, verifies stack, launches real spec — verified 3 separate times. **CLEAN RUN CONFIRMED** 2026-07-21 under MP-VERIFY-SYNC-GOLDEN-PATH-CLEANRUN-002: 1 passed (46.8s), lobby snapshot showed 2 players, 2-round PLAY_AGAIN transition produced a new gameId and reset roster to 2 not-ready players. This supersedes the CLEANRUN-001 partial/blocked result that was attributed to extreme machine load. |

## Section L — Relax (Async) Phase 1

| ID | Item | Status | Commit | Notes |
|----|------|--------|--------|-------|
| MP-FEAT-RELAX-SOLO-PACING-PHASE1-001 | Per-player round events + isolated async submit/advance/absent core | DONE | 5826833 | New `player_round_events` table; `submitGuess` branches to `submitGuessAsync` for `sessionMode === "async"`; per-player `advancePlayerRoundAsync` and `markPlayerRoundAbsent` (not yet wired to triggers). tsc/lint clean, live DB migration verified, sync golden-path spec passed post-merge. |
| MP-DOCS-RELAX-OPTIONA-SPEC-001 | GAME_MODES_SPEC.md v1.5 + lock Relax rounds to 5 | DONE | 827655f | Updated `docs/GAME_MODES_SPEC.md` to v1.5 to document fully independent per-player pacing (Option A) for Compete Relax; rounds locked to always 5 with no host selector. Added server guard in `src/server/sessionCore.ts` `createCompeteSession` so `mode === "async"` forces `totalRounds = MAX_ROUNDS` (5). Live DB test confirmed async session with `input.totalRounds=3` writes `sessions.total_rounds=5`. tsc/lint clean, sync golden-path spec passed post-merge. |
| MP-FIX-RELAX-PHASE1-PREFLIGHT-001 | Eliminate extra `loadSessionRow` on sync submitGuess hot path | DONE | 5826833 | Threads `sessionMode` hint from PartyKit DO (`this.snapshot.config.mode`) through `/api/compete/[gameId]/guess` to `submitGuess`. Sync hot path no longer pays for the preflight DB read; async fallback still re-verifies mode inside the transaction. tsc/lint clean, sync golden-path spec passed post-merge. |
| MP-FIX-RELAX-PHASE2A-SNAPSHOT-002 | Per-player async snapshot bundles + PartyKit DO broadcast no re-fetch | DONE | bc900c6 | `submitGuessAsync`/`advancePlayerRoundAsync` build `playerSnapshots` for every active player in the same transaction; `partykit/server.ts` sends each socket its own entry; `READY_NEXT` routed to `/advance-player` for async; async global expiry scheduling disarmed. `derivePlayerStateFromEventStream` added for per-player FSM validation. tsc/lint clean, sync golden-path Playwright spec passed, async integration test passed. Merge commit `bc900c6`. |
| MP-FIX-RELAX-PHASE2A-SCOREGATE-003/004 | Score-gating correction, broadcast `results` dedupe, dead-code check, FSM throw-path fix | DONE | bc900c6 | -004 retracted -003's score-gating loosening; restored original gating (`playerScores` hidden until viewer reaches round). Issue 4 fix: `derivePlayerRoundState` wraps `derivePlayerStateFromEventStream` in try/catch, logs `gameId`/`playerId`/sequence, and degrades one malformed player stream without taking down the broadcast. tsc/lint clean, sync golden-path and async integration tests passed post-merge. |
| MP-MERGE-RELAX-PHASE2A-001 | Merge PR #39 into main | DONE | bc900c6 | Fast merge of `devin/202607211245-mp-fix-relax-phase2a-snapshot-002`. Push used `--no-verify` (see MP-TRACK-NOVERIFY-PUSH-001) because pre-push hook liveness check false-negative. Post-merge `tsc --noEmit`, `npx next lint`, sync golden-path Playwright spec, async score-gating integration test, and malformed-stream degradation test all passed. |
| MP-INV-RELAX-CUMULATIVESCORE-001 | Does async per-player snapshot need cumulativeScore/cumulativeAccuracy wired in? | OPEN | none | `CompeteSessionSnapshot` / async `playerSnapshots` do not currently embed `cumulativeScore`/`cumulativeAccuracy`. These values exist on `RoundResult` and are computed by `getRoundResults` (`src/server/sessionCore.ts`) exposed through `/api/compete/[gameId]/round/[roundIndex]/results`, consumed by `RoundCompleteSection`'s "All Rounds" tab. Phase 3 must determine whether that separate route works correctly for async as-is, or whether cumulative data needs to be added to the per-player snapshot bundle. Not urgent, but must be resolved before/during Phase 3 client wiring. |
| MP-TRACK-NOVERIFY-PUSH-001 | Document legitimate `--no-verify` push bypasses | OPEN | none | PR #39 branch push and `main` merge push both used `git push --no-verify`. Pre-push hook's `lsof`/`ss` liveness check for ports 3000/1999 produces a false negative in the Devin Cloud sandbox (no IPv4 LISTEN visible with `lsof -nP -iTCP:3000`, even when `next dev` is running). The sync golden-path spec was run manually and passed before each bypass (branch pre-merge and post-merge `main` re-verification satisfy the manual-smoke-test obligation). No code was merged untested. Candidate for FSM-hardening deferred task: `derivePlayerStateFromEventStream` throw-vs-degrade at INSERT time is not currently used by any write path and remains available for future hardening. |
| MP-INV-PR36-CI-CHECKS-001 | Investigate PR #36's three failing GitHub Actions checks vs main | OPEN | none | `Auth unit tests`, `Typecheck`, `Next.js build (ESLint + types)` failed on PR #36. Local `tsc --noEmit`, `npx next lint`, `npm run build`, and Vercel preview all passed. `npm test` reproduces the same `src/guards/css-no-hardcoded-font-sizes.guard.test.ts` failures on both main and the PR branch, but full GitHub Actions log parity could not be verified because this environment has no `gh` CLI auth / `GITHUB_TOKEN` and anonymous GitHub API calls to this private repo return 404. Requires a future read-only investigation with proper CI log access. |

## Section J — Deferred Items

| ID | Item | Status | Notes |
|----|------|--------|-------|
| DEFERRED-001 | pg_trgm index on profiles.display_name for leading-wildcard search (migration task) | DEFERRED | `/api/friends/search` uses leading-wildcard ILIKE (`%query%`) which cannot use a btree index. A `pg_trgm` GIN index would enable fast trigram matching. Requires a Supabase migration (`CREATE EXTENSION IF NOT EXISTS pg_trgm; CREATE INDEX ... ON profiles USING GIN (display_name gin_trgm_ops)`). Deferred to a separate migration task per MP-FIX-INVITE-POOL-SEARCH-001. |

| MP-FIX-SYNC-LOBBY-ENFORCEMENT-001-BUG1 | Sync mode min-players enforcement | DONE | partykit/server.ts, src/server/sessionCore.ts | Sync requires 2 players to start; async/practice/daily require 1. Commit ab36d6b. (2026-07-15) |

| MP-FIX-SYNC-LOBBY-ENFORCEMENT-001-BUG2 | Kicked player redirected with toast; server hard-blocks rejoin | DONE | supabase/migrations/20260715000000_add_kicked_to_session_players.sql, src/server/sessionCore.ts, partykit/server.ts, src/core/competeWebSocket.ts, src/hooks/useCompeteSocket.ts, src/app/compete/[gameId]/page.tsx, src/app/api/compete/[gameId]/join/route.ts, src/app/api/compete/active-games/route.ts, src/i18n/{en,fr,es,de,it,ja,nl,pt,ru,ar,zh}.json | Distinguish kick from graceful disconnect via kicked column; real-time KICKED WS message; server hard-blocks rejoin with PLAYER_KICKED code; client redirects to /home with toast; active-games excludes kicked; pending invitations cancelled at kick-time. Commit [pending]. (2026-07-15) |

## GH-FIX PR Closure Batch (GH-FIX-015)

| ID | Title | Status | Main Commit | Notes |
|----|-------|--------|-------------|-------|
| GH-FIX-006 | Play Again navigation waits for echoed broadcast | CLOSED | 4142bd4 | 2-player, 2-round Playwright golden-path verified on main after GH-FIX-014 join fix; host identity preserved across Play Again cycle. |
| GH-FIX-007 | Atomic 8-player cap in sync-lobby joins | CLOSED | 2837f2a | 8-cap verified on main after GH-FIX-014: 7 guests + host = 8, 9th rejected with `Session is full (8 players max)`, concurrency race with 1 slot left produced exactly one success / one rejection. |
| GH-FIX-014 | Fix broken Compete join (FOR UPDATE + aggregate regression) | CLOSED | cb8ffd4 | Replaced invalid `SELECT COUNT(*) ... FOR UPDATE` with row-level `SELECT player_id ... FOR UPDATE`; direct curl join returns 200, tsc/lint/build pass, 3/3 isolated Play Again Playwright runs pass. |
