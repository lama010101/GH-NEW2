TASK ID: MP-UI-HOME-001
TITLE: Implement new home page UI
THINKING LEVEL: LOW

PROJECT: GUESS-HISTORY
REPO: D:\GH-NEW
DEPLOYMENT: gh-new2.vercel.app

---

CONTEXT

The home page must be replaced with a new design. The exact approved design is provided as a reference HTML file at:

  D:\GH-NEW\docs\home_page_reference.html

Open and study this file before writing any code. It is the visual and structural source of truth. Do not deviate from its layout, colors, card order, or panel behavior.

---

SCOPE — WHAT TO IMPLEMENT NOW

Wire exactly two interactions. Nothing else.

1. COMPETE card → "Go to Lobby" button
   - "Create lobby" option selected: navigate to the existing compete/lobby route (wherever the current lobby creation flow lives — find it in the repo, do not invent a new route)
   - "Join with code" option selected: reveal a code input field between the option buttons and the CTA. The input is full-width, centered, uppercase, max 8 characters. "Go to Lobby" is disabled and visually dimmed when this option is active and the input is empty. On submit with a valid code, navigate to the existing join-by-code route.

2. AVATAR (top-right circle) → profile/sign-out
   - Tapping the avatar opens a small dropdown or bottom sheet with exactly two options:
     a. "Profile" — navigate to the existing profile route (find it in the repo)
     b. "Sign out" — calls the existing sign-out function (find it in the repo, do not reimplement auth)
   - Keep it simple. A minimal absolute-positioned dropdown is fine. Match the dark glass aesthetic of the rest of the page (dark background, rgba border, white text).

ALL OTHER CARDS AND BUTTONS (Daily, Practice, Level Up) must render exactly as shown in the reference HTML but are NOT wired to any action yet. They are visual-only placeholders. Do not add TODO navigation or placeholder routes.

---

BACKGROUND MOSAIC

The reference HTML uses SVG placeholder cells for the mosaic grid. In the real implementation, replace the SVG placeholders with actual historical event images from the existing image pool (same source used in gameplay). Load 15 random images from Supabase storage. Apply object-fit: cover to each cell. If any image fails to load, fall back silently to the dark fill colors shown in the SVG placeholders. The overlay and grid structure must remain intact regardless of image load state.

---

IMPLEMENTATION RULES

- Implement this as the root home page of the Next.js app. Find the existing home page file (likely app/page.tsx or pages/index.tsx) and replace its content.
- The new home page must work for both authenticated and unauthenticated users. For unauthenticated users: render the page identically, but the XP pill shows "-- % | -- XP". Compete and avatar CTAs redirect to the sign-in page with a return URL.
- The XP pill (left side of top bar) is display-only. No click handler. No chevron. No navigation.
- The bell icon is display-only for now. No click handler.
- Practice card default selected on load (outline visible, panel shown). This is already the behavior in the reference HTML.
- Settings panel (timer toggle + year range) under Practice card: render it visually as shown. The toggles can fire state changes locally (toggle on/off animation works) but do not need to persist or affect any game session yet.
- Level Up "Level 5" pip on the card: hardcode to the authenticated player's current level from the session/auth context. If unavailable, show "Level 1".
- Do not import any new UI libraries. Use only what is already installed in the project.
- Do not modify any files in /docs/.
- One primary file change: the home page component. You may create one additional small component file if the dropdown/sheet for the avatar genuinely warrants it. No other files.

---

VALIDATION CRITERIA

Before marking complete, verify all of the following:

1. Vercel build passes (not just local build — deploy and confirm)
2. Home page renders on mobile (375px width) without horizontal scroll
3. Selecting each of the 4 cards updates the info panel with the correct content and the selected card lifts with the outline ring
4. "Go to Lobby" (Compete, Create lobby) navigates to the existing lobby creation flow
5. "Go to Lobby" (Compete, Join with code) is disabled when input is empty, enabled when input has content
6. Avatar tap opens the dropdown with Profile and Sign out
7. Sign out calls the existing auth sign-out and redirects correctly
8. Unauthenticated state renders without errors
9. Background mosaic loads real images (or falls back gracefully if none available)
10. XP pill has no click handler and no chevron

---

PROGRESS UPDATE

After completing this task, append the following entry to D:\GH-NEW\docs\PROGRESS.md:

  ## MP-UI-HOME-001 — New home page implemented
  Date: [today's date]
  Status: COMPLETE
  Files changed: [list files]
  Vercel build: CONFIRMED
  Notes: [any relevant observations]

---

REF: GUESS-HISTORY | MP-UI-HOME-001
